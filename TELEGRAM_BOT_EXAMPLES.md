# ThermoSystem Telegram Bot - Примеры использования

## 🚀 Быстрый старт

### 1. Базовая настройка

```bash
# 1. Клонирование репозитория
git clone <repository-url>
cd agents_for_david

# 2. Установка зависимостей
uv sync

# 3. Создание .env файла
cp .env.example .env
# Отредактируйте .env с вашими ключами

# 4. Запуск бота
uv run python run_telegram_bot.py
```

### 2. Файл .env

```bash
# Обязательные переменные
TELEGRAM_BOT_TOKEN=your_bot_token_here
OPENROUTER_API_KEY=your_openrouter_api_key

# Опциональные переменные
DB_PATH=data/thermo_data.db
ADMIN_USER_ID=123456789  # Ваш Telegram ID для административных команд
LOG_LEVEL=INFO
MAX_CONCURRENT_USERS=20
TELEGRAM_MODE=polling
TEMP_FILE_DIR=temp/telegram_files
```

## 💬 Примеры запросов к боту

### 📊 Табличные данные веществ

```
Дай таблицу для H2O при 300-600K с шагом 50 градусов
```

**Ответ:** Таблица с температурой, Cp, H, S, G для воды

```
Свойства CO2 от 298 до 1000K
```

**Ответ:** Термодинамические свойства CO₂ в указанном диапазоне

```
Термодинамические данные для Fe2O3 при 400-800K
```

**Ответ:** Детальная таблица свойств оксида железа(III)

```
NH3 свойства при 250-500K с шагом 25K
```

**Ответ:** Свойства аммиака с мелким шагом по температуре

### ⚗️ Расчёты реакций

```
2 H2 + O2 → 2 H2O при 298-1000K
```

**Ответ:** Термодинамический анализ реакции горения водорода

```
Fe2O3 + 3 C → 2 Fe + 3 CO при 800-1200K
```

**Ответ:** Расчёт реакции восстановления оксида железа

```
Реагирует ли сероводород с оксидом железа(II) при 500-700°C?
```

**Ответ:** Анализ спонтанности реакции и константы равновесия

```
WO3 + 3 H2 → W + 3 H2O при 600-900K
```

**Ответ:** Термодинамика восстановления вольфрама

```
N2 + 3 H2 → 2 NH3 при 400-700K, давление 100 атм
```

**Ответ:** Синтез аммиака (давление учитывается в расчётах)

## 🔧 Команды бота

### Базовые команды

```
/start     - Приветствие и краткая справка
/help      - Подробная справка по использованию
/status    - Статус бота и текущая нагрузка
/examples  - Примеры запросов
/about     - Информация о системе
```

### Административные команды (только для admin_user_id)

```
/admin_status     - Детальный статус системы
/admin_stats      - Статистика использования
/admin_health     - Проверка здоровья компонентов
/admin_users      - Активные пользователи
/admin_errors     - Ошибки системы
/admin_cleanup    - Очистка временных файлов
/admin_broadcast <сообщение>  - Рассылка сообщения
/admin_config     - Текущая конфигурация
/admin_system     - Системные ресурсы
```

## 📱 Интерактивные элементы

### Inline кнопки

После ответа бота доступны интерактивные кнопки:

- **💧 H₂O** - Быстрый расчёт свойств воды
- **🔥 Сгорание** - Расчёт реакции горения водорода
- **💨 CO₂** - Свойства углекислого газа
- **📄 Компактно** - Уплотнённый формат ответа
- **📊 Детально** - Развернутый формат ответа
- **🌡️ Расширить** - Расширить температурный диапазон
- **🔄 Повторить** - Повторить последний запрос
- **ℹ️ ThermoSystem** - Информация о системе
- **📝 Справка** - Помощь по использованию

## 📄 Формат результатов

### Сообщения (<3000 символов)

Для коротких результатов бот отправляет отформатированное сообщение с:

- 📊 Таблицами в формате Markdown
- 🧪 Химическими формулами в Unicode (H₂O, CO₂, →)
- ⚡ Эмодзи для визуальной структуры
- 📈 Интерпретацией результатов

### TXT файлы (>3000 символов)

Для детальных отчётов бот создаёт TXT файл с:

- 🔬 Полными таблицами результатов
- 📋 Детальной интерпретацией
- 📊 Метаданными расчёта
- ⏰ Временем создания
- 🎯 Профессиональным форматированием

## 🎯 Советы по использованию

### ✅ Правильные запросы

```
✅ 2 H2 + O2 → 2 H2O при 298-1000K
✅ H2O свойства при 300-600K
✅ CO2 от 298 до 1000K
✅ Fe2O3 + 3 C → 2 Fe + 3 CO
```

### ❌ Неправильные запросы

```
❌ 2h2+o2=2h2o  (используйте пробелы и →)
❌ H2O 300K     (укажите диапазон)
❌ водород      (используйте химические формулы)
❌ Слишком сложная органическая молекула
```

### 📋 Поддерживаемые форматы

- **Химические формулы:** H2O, CO2, NH3, CH4, Fe2O3, Al2O3
- **Реакции:** A + B → C + D (до 10 веществ)
- **Температуры:** 298K, 25°C, диапазоны 298-1000K
- **Шаги:** 25K, 50K, 100K, 250K
- **Давление:** можно указывать в атм, Па, бар

## 🔍 Диагностика проблем

### Проверка компонентов

```bash
# Только проверка, без запуска
uv run python run_telegram_bot.py --check

# Детальный вывод
uv run python run_telegram_bot.py --check --verbose
```

### Логи

```bash
# Просмотр логов
tail -f logs/telegram_bot.log

# Поиск ошибок
grep ERROR logs/telegram_bot.log
```

### Административный статус

Если вы настроили `ADMIN_USER_ID`, используйте:

```
/admin_status  - полный статус системы
/admin_health   - здоровье компонентов
/admin_errors   - последние ошибки
```

## 🛠️ Продвинутая настройка

### Конфигурация производительности

```python
# В коде или .env
MAX_CONCURRENT_USERS=50        # Увеличить одновременных пользователей
REQUEST_TIMEOUT_SECONDS=120    # Увеличить таймаут
RESPONSE_FORMAT_THRESHOLD=2000 # Отправлять файлы при 2000+ символах
```

### Webhook режим (продакшен)

```bash
# Переменные окружения
TELEGRAM_MODE=webhook
TELEGRAM_WEBHOOK_URL=https://your-domain.com/telegram

# Запуск с HTTPS
uv run python run_telegram_bot.py
```

### Мониторинг

```python
# health_check.py
import asyncio
from thermo_agents.telegram_bot.config import TelegramBotConfig
from thermo_agents.telegram_bot.utils.health_checker import HealthChecker

async def monitor():
    config = TelegramBotConfig.from_env()
    # ... инициализация компонентов
    health_results = await health_checker.check_all_components()
    print(f"Health score: {health_results['health_score']:.1f}%")

asyncio.run(monitor())
```

## 📞 Поддержка

При проблемах:

1. Проверьте `.env` файл
2. Убедитесь, что база данных существует
3. Проверьте логи в `logs/telegram_bot.log`
4. Используйте `--check` для диагностики
5. Для вопросов по ThermoSystem смотрите `docs/ARCHITECTURE.md`

---

**ThermoSystem Telegram Bot v1.1**
*Генерация термодинамических расчётов в Telegram*
*© 2025 - Проект ThermoSystem*