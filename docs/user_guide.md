# Руководство пользователя ThermoSystem v2.1

## Быстрый старт

### Установка
```bash
git clone <repository-url>
cd agents-for-david
uv sync
uv shell
```

### Основные запросы

#### Получение данных по веществу
```python
# Базовый запрос
"Дай таблицу для H2O при 300-600K"

# С кастомным шагом
"Свойства WCl6 с шагом 50 градусов при 400-1000K"

# Запрос с указанием фазы
"Табличные данные для CO2(g) при 500-1200K"
```

#### Расчёт реакции
```python
# Явное уравнение реакции
"2 W + 4 Cl2 + O2 → 2 WOCl4 при 600-900K"

# Описание процесса
"Рассчитай термодинамику хлорирования вольфрама"

# Разные типы стрелок
"H2 + Cl2 ⇄ 2 HCl при 400-800K"  # Обратимая реакция
"CH4 + 2 O2 -> CO2 + 2 H2O"     # Необратимая реакция
```

## Примеры вывода

### 1. Данные по веществу (Compound Data)

```
📊 Термодинамические данные: H2O

Базовые свойства:
  Формула: H2O
  Название: Water
  Фаза: g (gas (газ))
  Температурный диапазон: 298-1000 K
  H298 (энтальпия): -241.826 кДж/моль
  S298 (энтропия): 188.838 Дж/(моль·K)
  Точка плавления: 273.1 K (0.0°C)
  Точка кипения: 373.1 K (100.0°C)

Термодинамические свойства по температуре:
+--------+-------------------+---------------+------------------+---------------+
|   T(K) |   Cp(Дж/(моль·K)) |   H(кДж/моль) |   S(Дж/(моль·K)) |   G(кДж/моль) |
+========+===================+===============+==================+===============+
|    300 |             39.46 |       -241.75 |           189.08 |       -298.48 |
|    400 |             36.77 |       -237.88 |           199.77 |       -317.79 |
|    500 |             35.16 |       -234.02 |           208.05 |       -338.06 |
|    600 |             34.05 |       -230.20 |           214.79 |       -359.08 |
+--------+-------------------+---------------+------------------+---------------+

Примечания:
  - Шаг по температуре: 100 K
  - Все значения рассчитаны с использованием уравнений Шомейта
  - T: температура, Cp: теплоемкость, H: энтальпия, S: энтропия, G: энергия Гиббса
```

### 2. Расчёт реакции (Reaction Calculation)

```
⚗️ Термодинамический расчёт реакции

Уравнение реакции: 2 H₂ + O₂ → 2 H₂O

Метод расчёта:
1. Энтальпия реакции: ΔH°(T) = ΣH°_продукты - ΣH°_реагенты
2. Энтропия реакции: ΔS°(T) = ΣS°_продукты - ΣS°_реагенты
3. Энергия Гиббса: ΔG°(T) = ΔH°(T) - T·ΔS°(T)

Где:
  H°(T) = H°₂₉₈ + ∫₂₉₈ᵀ Cp(T)dT
  S°(T) = S°₂₉₈ + ∫₂₉₈ᵀ [Cp(T)/T]dT
  Cp(T) = f₁ + f₂T/1000 + f₃T⁻²·10⁵ + f₄T²/10⁶ + f₅T⁻³·10³ + f₆T³·10⁻⁹

Данные веществ:
H₂O — Water
  Фаза: g | T_применимости: 298-1000 K
  H₂₉₈: -241.826 кДж/моль | S₂₉₈: 188.838 Дж/(моль·K)
  Cp коэффициенты: [30.092000, 6.832514, 6.793435, -2.534480, 0.082139, -0.028522]

H₂ — Hydrogen
  Фаза: g | T_применимости: 298-1000 K
  H₂₉₈: 0.000 кДж/моль | S₂₉₈: 130.681 Дж/(моль·K)
  Cp коэффициенты: [33.066178, -11.363417, 11.432816, -2.772874, -0.158558, 0.000000]

O₂ — Oxygen
  Фаза: g | T_применимости: 298-1000 K
  H₂₉₈: 0.000 кДж/моль | S₂₉₈: 205.152 Дж/(моль·K)
  Cp коэффициенты: [31.322340, -20.235310, 57.866440, -36.506240, 0.000000, 0.000000]

Результаты расчёта:
T(K)     | ΔH°(кДж/моль) | ΔS°(Дж/(К·моль)) | ΔG°(кДж/моль) | Комментарий
----------------------------------------------------------------------
    298 |        -479.46 |           -167.34 |        -429.50 | Экзергоническая (⇑ спонтанная)
    398 |        -482.19 |           -165.23 |        -416.44 | Экзергоническая (⇑ спонтанная)
    498 |        -484.92 |           -163.12 |        -403.60 | Экзергоническая (⇑ спонтанная)
    598 |        -487.64 |           -161.01 |        -391.37 | Экзергоническая (⇑ спонтанная)

Шаг по температуре: 100 K
Расчёты выполнены с использованием уравнений Шомейта
```

## Синтаксис запросов

### Температурные диапазоны
- **Формат:** `при T_min-T_maxK` или `от T_min до T_max K`
- **Примеры:** `при 300-600K`, `от 400 до 1200 K`
- **Диапазон:** 25-6000K (зависит от данных в базе)

### Шаг температуры
- **Формат:** `с шагом N градусов/кельвинов`
- **По умолчанию:** 100K
- **Допустимые значения:** 25-250K
- **Примеры:** `с шагом 50 градусов`, `каждые 25 кельвинов`

### Фазовые состояния
- **Формат:** `Формула(фаза)`
- **Фазы:** `s` (твёрдое), `l` (жидкость), `g` (газ), `aq` (раствор)
- **Примеры:** `H2O(l)`, `CO2(g)`, `Fe(s)`

### Химические уравнения
- **Реагенты и продукты:** разделение стрелками `→`, `->`, `⇄`, `=`
- **Стехиометрические коэффициенты:** перед формулой `2 H2O`
- **Поддерживаемые реакции:** окисление, восстановление, синтез, разложение

## Обработка ошибок

### Вещество не найдено
```
❌ Вещество 'XYZ123' не найдено в базе данных

Возможные причины:
  - Неверная формула или название вещества
  - Вещество отсутствует в термодинамической базе данных
  - Неправильный формат запроса

Попробуйте:
  - Проверить правильность написания формулы
  - Использовать альтернативное название вещества
  - Указать фазовое состояние (s, l, g, aq)
```

### Температура вне диапазона
```
❌ Не найдено записей для H2O в диапазоне 100-200K

Рекомендуемый диапазон для H2O(g): 298.15-1000.0 K
```

### Некорректный шаг температуры
```
❌ Шаг температуры должен быть в диапазоне 25-250K, получено: 10

Используйте шаги от 25K до 250K для оптимальной точности
```

## Советы по использованию

### Для повышения точности
1. **Используйте небольшие шаги** (25-50K) для критических областей
2. **Указывайте фазовое состояние** при неоднозначности
3. **Проверяйте температурные диапазоны** для конкретных веществ

### Для быстрых расчётов
1. **Используйте стандартный шаг** 100K
2. **Запрашивайте ограниченные диапазоны** (не более 1000K размах)
3. **Используйте простые формулы** без изотопов

### Для анализа реакций
1. **Проверяйте наличие всех веществ** в базе данных
2. **Используйте сбалансированные уравнения** реакций
3. **Анализируйте знак ΔG°** для определения спонтанности

## Ограничения системы

- **База данных:** Ограничена доступными соединениями (~316K записей)
- **Температуры:** Расчёты только в пределах данных Tmin-Tmax
- **Фазы:** Не поддерживает метастабильные состояния
- **Давление:** Все расчёты для стандартного давления (1 атм)

## Интеграция с кодом

### Python API
```python
from thermo_agents.orchestrator import Orchestrator
from thermo_agents.search.database_connector import DatabaseConnector
from thermo_agents.search.compound_searcher import CompoundSearcher
from thermo_agents.search.sql_builder import SQLBuilder
from thermo_agents.filtering.filter_pipeline import FilterPipeline
from thermo_agents.thermodynamic_agent import ThermodynamicAgent, ThermoAgentConfig

# Инициализация
orchestrator = Orchestrator(
    thermodynamic_agent=agent,
    compound_searcher=searcher,
    filter_pipeline=filter_pipeline
)

# Запрос
result = await orchestrator.process_query("H2O при 300-600K")
print(result)
```

### Массовые запросы
```python
queries = [
    "H2O при 300-500K",
    "CO2 при 400-800K",
    "CH4 при 500-900K"
]

results = []
for query in queries:
    result = await orchestrator.process_query(query)
    results.append(result)
```

## Поддержка

Для получения помощи:
1. Проверьте примеры в `examples/`
2. Изучите тесты в `tests/`
3. Обратитесь к документации API в `docs/api_reference.md`