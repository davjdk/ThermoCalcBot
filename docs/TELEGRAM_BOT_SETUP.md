# ThermoSystem Telegram Bot - Руководство по настройке и использованию

## 📋 Обзор

ThermoSystem Telegram Bot (@ThermoCalcBot) предоставляет доступ к функциям термодинамических расчётов через мессенджер Telegram. Бот поддерживает:

- 📊 Получение табличных данных термодинамических свойств веществ
- ⚗️ Расчёт термодинамики химических реакций
- 📄 Генерацию детальных TXT файлов с результатами
- 🌡️ Многофазные расчёты с учётом фазовых переходов

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
# Установка зависимостей
uv sync

# Активация виртуального окружения
uv shell
```

### 2. Настройка переменных окружения

Скопируйте файл примера и заполните реальные значения:

```bash
cp .env.example .env
```

Отредактируйте `.env` файл:

```bash
# Токен Telegram бота (ОБЯЗАТЕЛЬНО)
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here

# LLM API конфигурация (ОБЯЗАТЕЛЬНО)
OPENROUTER_API_KEY=your_openrouter_api_key_here
LLM_BASE_URL=https://openrouter.ai/api/v1
LLM_DEFAULT_MODEL=openai/gpt-4o

# База данных ThermoSystem
DB_PATH=data/thermo_data.db
STATIC_CACHE_DIR=data/static_compounds

# Директории
TEMP_FILE_DIR=temp/telegram_files
TELEGRAM_SESSION_LOG_DIR=logs/telegram_sessions

# Логирование
LOG_LEVEL=INFO
```

### 3. Получение токена Telegram бота

1. Найдите [@BotFather](https://t.me/botfather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям:
   - Введите имя бота: `ThermoSystem Calculator`
   - Введите username: `ThermoCalcBot` (или другой)
4. Сохраните полученный токен в `TELEGRAM_BOT_TOKEN`

### 4. Получение API ключа для LLM

1. Зарегистрируйтесь на [OpenRouter](https://openrouter.ai/)
2. Получите API ключ в настройках профиля
3. Сохраните ключ в `OPENROUTER_API_KEY`

### 5. Запуск бота

```bash
# Запуск бота
uv run python telegram_bot.py
```

После запуска бот должен появиться в Telegram и будет готов принимать запросы.

## 📝 Использование бота

### Основные команды

- `/start` - Приветствие и краткая справка
- `/help` - Подробная справка по использованию
- `/status` - Статус бота и текущая нагрузка
- `/examples` - Примеры запросов
- `/about` - Информация о системе

### Примеры запросов

**Табличные данные веществ:**
```
Дай таблицу для H2O при 300-600K с шагом 50 градусов
Свойства CO2 от 298 до 1000K
Термодинамические данные для Fe2O3 при 400-800K
```

**Расчёты реакций:**
```
2 H2 + O2 → 2 H2O при 298-1000K
Fe2O3 + 3 C → 2 Fe + 3 CO при 800-1200K
Реагирует ли сероводород с оксидом железа(II) при 500-700°C?
WO3 + 3 H2 → W + 3 H2O при 600-900K
```

### Формат результатов

- **Короткие результаты** (<3000 символов) отправляются как сообщения
- **Детальные отчёты** (>3000 символов) отправляются как TXT файлы
- Максимальный размер файла: 20MB
- Поддержка Unicode химических формул: H₂O, CO₂, →, ΔH, ΔG

## 🛠️ Конфигурация

### Основные параметры

- `MAX_CONCURRENT_USERS` - Максимум одновременных пользователей (по умолчанию: 20)
- `REQUEST_TIMEOUT_SECONDS` - Таймаут запросов к LLM (по умолчанию: 90)
- `MAX_FILE_SIZE_MB` - Максимальный размер файла (по умолчанию: 20)
- `RATE_LIMIT_MESSAGES_PER_MINUTE` - Лимит сообщений в минуту (по умолчанию: 30)

### Директории

- `TEMP_FILE_DIR` - Временные файлы для TXT отчётов
- `TELEGRAM_SESSION_LOG_DIR` - Логи сессий пользователей
- `LOG_LEVEL` - Уровень логирования (DEBUG, INFO, WARNING, ERROR)

## 🔍 Мониторинг и отладка

### Просмотр логов

```bash
# Логи бота
tail -f logs/telegram_bot.log

# Логи сессий пользователей
ls logs/telegram_sessions/
```

### Статистика бота

Команда `/status` показывает:
- Количество активных пользователей
- Общее количество запросов
- Среднее время ответа
- Использование CPU и памяти
- Количество временных файлов

### Health check

Бот автоматически проверяет здоровье компонентов:
- ThermoOrchestrator
- База данных SQLite
- LLM API доступность

## 🧪 Тестирование

### Запуск тестов

```bash
# Базовые тесты функциональности
uv run pytest tests/telegram_bot/ -v

# Конкретный тест
uv run pytest tests/telegram_bot/test_basic_functionality.py::TestTelegramBotBasics::test_config_validation -v
```

### Тестирование компонентов

- `test_config_validation` - Валидация конфигурации
- `test_file_handler_*` - Работа с файлами
- `test_response_formatter_*` - Форматирование ответов
- `test_session_manager_*` - Управление сессиями
- `test_rate_limiter_*` - Ограничения запросов

## 🔒 Безопасность

### Защита токена

- **НИКОГДА** не коммитьте `TELEGRAM_BOT_TOKEN` в git
- Храните токен только в переменных окружения
- Используйте разные токены для разработки и продакшена

### Ограничения доступа

- Rate limiting для предотвращения спама
- Лимиты одновременных пользователей
- Автоматическая очистка временных файлов
- Логирование только ID пользователей (без персональных данных)

## 📊 Производительность

### Ожидаемые метрики

- **Время ответа:** <10 секунд для сложных расчётов
- **Конкурентность:** До 20 одновременных пользователей
- **Надежность:** 99.9% uptime
- **Размер TXT файлов:** До 20MB

### Оптимизация

- Асинхронная обработка запросов
- Кэширование результатов
- Управление временными файлами
- Graceful degradation при ошибках

## 🐣 Устранение неполадок

### Частые проблемы

**Бот не запускается:**
```bash
# Проверка токена
curl -X GET "https://api.telegram.org/bot{TOKEN}/getMe"

# Проверка конфигурации
uv run python -c "from thermo_agents.telegram_bot.config import TelegramBotConfig; print(TelegramBotConfig.from_env().validate_config())"
```

**Ошибки LLM API:**
- Проверьте `OPENROUTER_API_KEY`
- Убедитесь что API ключ активен
- Проверьте лимиты запросов

**Проблемы с базой данных:**
- Убедитесь что файл `data/thermo_data.db` существует
- Проверьте права доступа к файлу
- Проверьте целостность базы данных

**Ошибки с файлами:**
- Проверьте права на запись в `temp/telegram_files`
- Убедитесь что достаточно места на диске
- Проверьте лимиты размера файлов

### Логирование ошибок

При возникновении проблем проверьте:
- `logs/telegram_bot.log` - основные логи бота
- `logs/telegram_sessions/` - логи сессий пользователей
- Статус бота через команду `/status`

## 🤝 Поддержка

При возникновении проблем:

1. Проверьте логи на наличие ошибок
2. Убедитесь что все переменные окружения настроены корректно
3. Попробуйте перезапустить бота
4. Проверьте статус компонентов через `/status`

---

**Версия:** 1.1 (9 ноября 2025)
**Документация:** [Telegram Bot Integration Specification](docs/specs/telegram_bot_integration/)
**Архитектура:** [ThermoSystem Architecture](docs/ARCHITECTURE.md)