# Этап 2: Внедрение core-логики из calc_example.ipynb

**Дата создания:** 2025-11-02  
**Статус:** Не начат  
**Приоритет:** Высокий  
**Зависимости:** Завершение этапа 1  

---

## Цель этапа

Внедрить проверенную логику отбора записей из БД и термодинамических расчетов из ноутбука `docs/calc_example.ipynb` в основную кодовую базу с полным логированием всех этапов.

---

## Мотивация

**Почему логика из ноутбука лучше:**

1. **Простота и прозрачность:** Алгоритм понятен без углубления в множество классов
2. **Проверенность:** Логика протестирована на реальных данных из логов
3. **Двухстадийный поиск:** Четкая стратегия поиска (формула+имя → только формула)
4. **Трехуровневая стратегия отбора записей:** Понятная приоритизация при построении температурного диапазона
5. **Корректный учет фазовых переходов:** Автоматическое переключение фаз при достижении MeltingPoint/BoilingPoint

**Текущая проблема:**

После этапа 1 система умеет только парсить запросы, но не выполняет расчеты. Нужно восстановить функциональность на базе улучшенной логики.

---

## Архитектура новых модулей

### Структура директорий:

```
src/thermo_agents/
├── core_logic/                          # ← НОВАЯ ДИРЕКТОРИЯ
│   ├── __init__.py
│   ├── compound_data_loader.py          # Двухстадийный поиск в БД
│   ├── phase_transition_detector.py     # Определение MeltingPoint/BoilingPoint
│   ├── record_range_builder.py          # Трехуровневая стратегия отбора записей
│   ├── thermodynamic_engine.py          # Расчет Cp, H, S, G для одного вещества
│   └── reaction_engine.py               # Расчет ΔH, ΔS, ΔG, K для реакции
```

### Диаграмма потока данных:

```
[LLM Response]
     ↓
[Orchestrator] → extract_parameters()
     ↓
[CompoundDataLoader]
     ├─→ [StaticDataManager] → YAML cache (H2O, CO2, O2, NH3, etc.) [Приоритет!]
     └─→ [DatabaseConnector] → двухстадийный поиск в БД (если нет в YAML)
           ├─→ СТАДИЯ 1: формула + имя
           └─→ СТАДИЯ 2: только формула (fallback)
     ↓
[PhaseTransitionDetector] → get_most_common_melting_boiling_points()
     ↓
[RecordRangeBuilder] → get_compound_records_for_range() (трехуровневая стратегия)
     ↓
[ThermodynamicEngine] → calculate_thermodynamic_properties() (для каждого вещества)
     ↓
[ReactionEngine] → calculate_reaction_thermodynamics() (для всей реакции)
     ↓
[Formatter] → format_result()
     ↓
[User Output]
```

---

## Спецификация модулей

### 1. `compound_data_loader.py`

**Назначение:** Поиск записей вещества с приоритетом YAML-кэша и двухстадийным поиском в БД

**Ключевые функции:**

```python
class CompoundDataLoader:
    """
    Загрузчик данных веществ с YAML-кэшем и двухстадийной стратегией поиска в БД.
    """
    
    def __init__(
        self, 
        db_connector: DatabaseConnector, 
        static_data_manager: StaticDataManager,
        logger: logging.Logger
    ):
        self.db_connector = db_connector
        self.static_manager = static_data_manager
        self.logger = logger
    
    def get_raw_compound_data(
        self, 
        formula: str, 
        compound_names: Optional[List[str]] = None
    ) -> pd.DataFrame:
        """
        Трехстадийный поиск вещества (YAML → БД формула+имя → БД только формула).
        
        СТАДИЯ 0 (приоритет): Проверка YAML-кэша для распространенных веществ
        СТАДИЯ 1: Поиск в БД по формуле + первое имя (строгое соответствие)
        СТАДИЯ 2 (fallback): Поиск в БД только по формуле
        
        Args:
            formula: Химическая формула (например, "SO2", "H2O")
            compound_names: Список имен из LLM response (опционально)
        
        Returns:
            DataFrame со всеми найденными записями, отсортированными по:
            - ReliabilityClass (1 > 2 > 3 > 0 > 4 > 5)
            - Tmax - Tmin (больший диапазон лучше)
            - Длина формулы (короче лучше)
            - Фаза (g > l > s > aq)
        
        Логирование:
            - ⚡ {formula}: найдено в YAML-кэше ({N} фаз)
            - ✓ {formula} (стадия 1: формула + '{first_name}'): найдено N записей
            - ⚠ {formula}: стадия 1 не дала результатов, переход к стадии 2
            - ✓ {formula} (стадия 2: только формула): найдено N записей
            - ⚠ {formula}: не найдено записей (все стадии)
        """
        # Стадия 0: YAML-кэш (для H2O, CO2, O2, NH3, Cl2, HCl, NaCl, FeO, C, CO)
        if self.static_manager.is_available(formula):
            self.logger.info(f"⚡ {formula}: найдено в YAML-кэше")
            yaml_data = self.static_manager.load_compound(formula)
            return self._convert_yaml_to_dataframe(yaml_data)
        
        # Стадия 1: БД с формулой + именем
        # ...
        
        # Стадия 2: БД только формула
        # ...
```

**Преимущества YAML-кэша:**

- 🚀 **Мгновенный доступ** для 10+ распространенных веществ (H2O, CO2, O2, NH3, etc.)
- 📊 **Предвычисленные данные**: фазовые переходы уже определены
- ✅ **Надежность**: проверенные данные с указанием источников (Glushko 94, Barin 93)
- 💾 **Кэширование**: повторные запросы не требуют файловых операций

**Поддерживаемые вещества в YAML-кэше:**

```
data/static_compounds/
├── H2O.yaml    # Вода (3 фазы: s, l, g)
├── CO2.yaml    # Углекислый газ
├── CO.yaml     # Угарный газ
├── O2.yaml     # Кислород
├── NH3.yaml    # Аммиак
├── Cl2.yaml    # Хлор
├── HCl.yaml    # Хлороводород
├── NaCl.yaml   # Хлорид натрия
├── FeO.yaml    # Оксид железа
└── C.yaml      # Углерод (графит)
```

**SQL запросы для БД (портировать из ноутбука):**

```sql
-- СТАДИЯ 1: Формула + имя
SELECT * FROM compounds
WHERE (
    (TRIM(Formula) = '{formula}' OR Formula LIKE '{formula}(%')
    AND (TRIM(FirstName) = '{first_name}' OR TRIM(SecondName) = '{first_name}')
)
AND (Formula NOT LIKE '%+%' AND Formula NOT LIKE '%-%')
ORDER BY 
    CASE ReliabilityClass 
        WHEN 1 THEN 0 WHEN 2 THEN 1 WHEN 3 THEN 2 
        WHEN 0 THEN 3 WHEN 4 THEN 4 WHEN 5 THEN 5 ELSE 6 
    END,
    (Tmax - Tmin) DESC,
    LENGTH(TRIM(Formula)) ASC,
    CASE Phase 
        WHEN 'g' THEN 0 WHEN 'l' THEN 1 
        WHEN 's' THEN 2 WHEN 'aq' THEN 3 ELSE 4 
    END,
    rowid ASC

-- СТАДИЯ 2: Только формула
SELECT * FROM compounds
WHERE (TRIM(Formula) = '{formula}' OR Formula LIKE '{formula}(%')
AND (Formula NOT LIKE '%+%' AND Formula NOT LIKE '%-%')
ORDER BY ... (те же критерии)
```

---

### 2. `phase_transition_detector.py`

**Назначение:** Определение точек фазовых переходов

**Ключевые функции:**

```python
class PhaseTransitionDetector:
    """
    Определяет температуры фазовых переходов (плавление, кипение).
    """
    
    @staticmethod
    def get_most_common_melting_boiling_points(df: pd.DataFrame) -> Tuple[Optional[float], Optional[float]]:
        """
        Находит самые частые ненулевые значения MeltingPoint и BoilingPoint.
        
        Фильтрация:
        - Игнорируются нулевые значения
        - BoilingPoint должен быть > MeltingPoint
        
        Args:
            df: DataFrame с данными вещества (колонки: MeltingPoint, BoilingPoint)
        
        Returns:
            (melting_point, boiling_point) или (None, None)
        
        Логирование:
            - ✓ Определены точки переходов: melting={value}K, boiling={value}K
            - ⚠ Точки переходов не определены для {formula}
        """
```

**Логика (из ноутбука):**

```python
melting_point = df[df['MeltingPoint'] != 0]['MeltingPoint'].value_counts().idxmax() if (df['MeltingPoint'] != 0).any() else None

if melting_point is not None:
    boiling_candidates = df[(df['BoilingPoint'] != 0) & (df['BoilingPoint'] > melting_point)]['BoilingPoint']
else:
    boiling_candidates = df[df['BoilingPoint'] != 0]['BoilingPoint']

boiling_point = boiling_candidates.value_counts().idxmax() if len(boiling_candidates) > 0 else None
```

---

### 3. `record_range_builder.py`

**Назначение:** Трехуровневая стратегия отбора записей для температурного диапазона

**Ключевые функции:**

```python
class RecordRangeBuilder:
    """
    Строит список записей для покрытия температурного диапазона.
    """
    
    def __init__(self, logger: logging.Logger):
        self.logger = logger
    
    def get_compound_records_for_range(
        self,
        df: pd.DataFrame,
        t_range: List[float],  # [T_start, T_end]
        melting: Optional[float],
        boiling: Optional[float],
        tolerance: float = 1.0
    ) -> List[pd.Series]:
        """
        Возвращает список записей, покрывающих весь температурный диапазон.
        
        Трехуровневая стратегия поиска:
        
        СТРАТЕГИЯ 1 (приоритет):
        - Ищем запись для ожидаемой фазы с Tmin ≈ current_T (±tolerance)
        
        СТРАТЕГИЯ 2 (fallback):
        - Ищем любую запись с Tmin ≈ current_T
        - Проверяем, что >50% диапазона в правильной фазе
        
        СТРАТЕГИЯ 3 (последний шанс):
        - Ищем запись, которая ПОКРЫВАЕТ current_T (Tmin < current_T < Tmax)
        - Проверяем, что >50% ОСТАВШЕГОСЯ диапазона в правильной фазе
        
        Учёт фазовых переходов:
        - При достижении melting/boiling переключается на соответствующую фазу
        - Продолжает поиск с температуры перехода
        
        Args:
            df: DataFrame с записями вещества
            t_range: [T_start, T_end]
            melting: Температура плавления (K)
            boiling: Температура кипения (K)
            tolerance: Допустимое отклонение для Tmin (K)
        
        Returns:
            Список записей (pd.Series), покрывающих весь диапазон
        
        Логирование:
            - [Стратегия 1] Использована запись фазы '{phase}' при T={T}K
            - [Стратегия 2] Использована запись фазы '{phase}' (доминирует {dom_phase}) при T={T}K
            - [Стратегия 3] Использована покрывающая запись фазы '{phase}' (Tmin={Tmin}, покрывает T={T}K)
            - Фазовый переход при T={transition_T}K внутри записи {phase}
            - ⚠ Не найдена подходящая запись для T={T}K (ожидаемая фаза: {phase})
        """
    
    @staticmethod
    def _get_phase_at_temperature(T: float, melting: Optional[float], boiling: Optional[float]) -> str:
        """Определяет ожидаемую фазу при температуре T."""
        if boiling and T >= boiling:
            return 'g'
        elif melting and T >= melting:
            return 'l'
        else:
            return 's'
    
    @staticmethod
    def _get_dominant_phase(record: pd.Series, melting: Optional[float], boiling: Optional[float]) -> str:
        """Определяет фазу, в которой запись находится >50% времени."""
        # Логика из ноутбука
    
    @staticmethod
    def _get_phase_fraction_from_T(
        record: pd.Series, 
        start_T: float, 
        melting: Optional[float], 
        boiling: Optional[float]
    ) -> float:
        """Вычисляет долю диапазона [start_T, Tmax] в фазе записи."""
        # Логика из ноутбука
```

**Особенности реализации:**

1. **Проверка последовательности фаз:** Запрещены переходы s → g (пропуск жидкой фазы)
2. **Приоритизация стратегий:** Сортировка кандидатов по приоритету (1 < 2 < 3)
3. **Детальное логирование:** Каждая стратегия логирует свой выбор

---

### 4. `thermodynamic_engine.py`

**Назначение:** Расчет термодинамических свойств для одного вещества

**Ключевые функции:**

```python
class ThermodynamicEngine:
    """
    Расчет Cp, H, S, G для одного вещества при заданной температуре.
    """
    
    def __init__(self, logger: logging.Logger):
        self.logger = logger
        self.T_ref = 298.15  # Референсная температура (K)
    
    def calculate_properties(
        self,
        record: pd.Series,
        T: float
    ) -> Dict[str, float]:
        """
        Расчет термодинамических свойств при температуре T.
        
        Формулы:
        
        Cp(T) = f₁ + f₂·T/1000 + f₃·T⁻²·10⁵ + f₄·T²/10⁶ + f₅·T⁻³·10³ + f₆·T³·10⁻⁹
        
        ΔH = ∫₂₉₈ᵀ Cp(T) dT  (численное интегрирование)
        H(T) = H₂₉₈ + ΔH
        
        ΔS = ∫₂₉₈ᵀ [Cp(T)/T] dT  (численное интегрирование)
        S(T) = S₂₉₈ + ΔS
        
        G(T) = H(T) - T·S(T)
        
        Args:
            record: Строка DataFrame с коэффициентами (f1-f6, H298, S298, Tmin, Tmax)
            T: Температура расчета (K)
        
        Returns:
            {
                'cp': теплоемкость (Дж/(моль·K)),
                'enthalpy': энтальпия (Дж/моль),
                'entropy': энтропия (Дж/(моль·K)),
                'gibbs_energy': энергия Гиббса (Дж/моль)
            }
        
        Предупреждения:
            - ⚠ Температура {T}K выходит за пределы {Tmin}-{Tmax}K для {formula}
        
        Численное интегрирование:
            - Метод: трапеций (np.trapz)
            - Точек интегрирования: 100
        """
```

**Портирование из ноутбука:**

Функция `calculate_thermodynamic_properties()` полностью портируется с минимальными изменениями (добавляется логирование).

---

### 5. `reaction_engine.py`

**Назначение:** Расчет термодинамики реакции

**Ключевые функции:**

```python
class ReactionEngine:
    """
    Расчет ΔH, ΔS, ΔG, K для химической реакции.
    """
    
    def __init__(
        self,
        compound_loader: CompoundDataLoader,
        phase_detector: PhaseTransitionDetector,
        range_builder: RecordRangeBuilder,
        thermo_engine: ThermodynamicEngine,
        logger: logging.Logger
    ):
        self.compound_loader = compound_loader
        self.phase_detector = phase_detector
        self.range_builder = range_builder
        self.thermo_engine = thermo_engine
        self.logger = logger
        self.R = 8.314  # Дж/(моль·K)
    
    def calculate_reaction(
        self,
        params: ExtractedReactionParameters,
        temperature_range: List[float]  # [T_start, T_end, step]
    ) -> pd.DataFrame:
        """
        Рассчитывает термодинамику реакции для диапазона температур.
        
        Алгоритм:
        
        1. Для каждого вещества:
           - Загрузить данные из БД (двухстадийный поиск)
           - Определить MeltingPoint, BoilingPoint
           - Получить записи для диапазона [T_start, T_end]
        
        2. Для каждой температуры T:
           - Для каждого вещества:
             * Найти подходящую запись (Tmin ≤ T ≤ Tmax)
             * Рассчитать H(T), S(T)
             * Умножить на стехиометрический коэффициент
           - Суммировать вклады:
             ΔH(T) = Σ [coeff · H(T)]
             ΔS(T) = Σ [coeff · S(T)]
           - Вычислить ΔG(T) = ΔH(T) - T·ΔS(T)
           - Вычислить ln(K) = -ΔG(T) / (R·T)
           - Вычислить K = exp(ln(K))
        
        3. Вернуть DataFrame с колонками:
           T (K), ΔH (Дж/моль), ΔS (Дж/(моль·K)), ΔG (Дж/моль), ln(K), K
        
        Args:
            params: Параметры из LLM (формулы, стехиометрия, уравнение)
            temperature_range: [T_start, T_end, step] в K
        
        Returns:
            DataFrame с результатами расчета
        
        Raises:
            ValueError: Если не найдены данные для одного из веществ
        
        Логирование:
            - ═══ РАСЧЕТ РЕАКЦИИ: {equation}
            - Стехиометрия: {stoichiometry}
            - ✓ {formula}: подготовлено {N} записей, coeff={coeff}
            - ⚠ T={T}K: нет подходящей записи для {formula}
            - ✓ Расчет завершен: {N} температурных точек
        """
    
    def parse_reaction_equation(
        self,
        equation: str,
        all_compounds: List[str]
    ) -> Dict[str, float]:
        """
        Парсит уравнение реакции и извлекает стехиометрические коэффициенты.
        
        Поддерживаемые форматы:
        - "A + 2B = C"
        - "A + 2B → C"
        - "A + 2B -> C"
        
        Args:
            equation: Сбалансированное уравнение
            all_compounds: Список формул веществ
        
        Returns:
            {formula: coefficient}
            Реагенты (левая сторона): отрицательные коэффициенты
            Продукты (правая сторона): положительные коэффициенты
        
        Пример:
            "2H2 + O2 → 2H2O"
            → {'H2': -2.0, 'O2': -1.0, 'H2O': 2.0}
        """
```

**Портирование из ноутбука:**

Функции `calculate_reaction_thermodynamics()` и `parse_reaction_equation()` портируются с добавлением логирования.

---

## Интеграция с `orchestrator.py`

### Обновленный метод `process_query()`:

```python
async def process_query(self, user_query: str) -> str:
    """
    Обработка запроса с использованием новой core-логики.
    """
    try:
        # 1. Логирование запроса
        if self.session_logger:
            self.session_logger.log_llm_request(user_query)
        
        # 2. Извлечение параметров через LLM
        params = await self.thermodynamic_agent.extract_parameters(user_query)
        
        # 3. Логирование ответа LLM
        if self.session_logger:
            self.session_logger.log_llm_response(params)
        
        # 4. Расчет реакции через новый ReactionEngine
        if params.query_type == "reaction_calculation":
            temperature_range = [298, 2500, 100]  # Фиксированный диапазон
            
            df_result = self.reaction_engine.calculate_reaction(
                params, temperature_range
            )
            
            # 5. Временное форматирование (будет улучшено на этапе 3)
            return self._format_temporary_result(df_result, params)
        
        else:
            return "⚠️ Обработка compound_data запросов будет добавлена позже"
    
    except Exception as e:
        self.logger.error(f"Ошибка обработки запроса: {e}")
        if self.session_logger:
            self.session_logger.log_error(str(e))
        return f"❌ Ошибка: {str(e)}"
```

### Инициализация компонентов в `__init__()`:

```python
def __init__(self, config: ThermoOrchestratorConfig, session_logger: Optional[SessionLogger] = None):
    # ... существующий код ...
    
    # Новые core-компоненты
    self.compound_loader = CompoundDataLoader(self.db_connector, self.logger)
    self.phase_detector = PhaseTransitionDetector()
    self.range_builder = RecordRangeBuilder(self.logger)
    self.thermo_engine = ThermodynamicEngine(self.logger)
    self.reaction_engine = ReactionEngine(
        self.compound_loader,
        self.phase_detector,
        self.range_builder,
        self.thermo_engine,
        self.logger
    )
```

---

## Логирование

### Уровни логирования:

1. **INFO:**
   - Начало/конец расчета
   - Успешное завершение этапов
   - Количество найденных записей

2. **DEBUG:**
   - Детали SQL запросов
   - Промежуточные значения расчетов
   - Стратегии отбора записей

3. **WARNING:**
   - Отсутствие данных для вещества
   - Выход температуры за пределы применимости
   - Fallback на стратегию 2/3

4. **ERROR:**
   - Критические ошибки расчета
   - Отсутствие записей для вещества

### Формат логов (сохранить совместимость с `session_logger.py`):

```
================================================================================
[РАСЧЕТ РЕАКЦИИ] 2025-11-02 15:30:00
================================================================================
Уравнение: MgO + SO2 → MgSO3
Температурный диапазон: 298-2500 K
Шаг: 100 K

--------------------------------------------------------------------------------
[ЗАГРУЗКА ДАННЫХ]
--------------------------------------------------------------------------------
✓ MgO (стадия 1: формула + 'Magnesium oxide'): найдено 22 записей
✓ SO2 (стадия 2: только формула): найдено 99 записей
⚠ MgSO3: не найдено записей (обе стадии)

❌ ОШИБКА: Не найдены данные для MgSO3

================================================================================
```

---

## План миграции

### Шаг 1: Создать новые модули

```powershell
# Создать директорию
New-Item -ItemType Directory -Path src/thermo_agents/core_logic

# Создать файлы
New-Item -ItemType File -Path src/thermo_agents/core_logic/__init__.py
New-Item -ItemType File -Path src/thermo_agents/core_logic/compound_data_loader.py
New-Item -ItemType File -Path src/thermo_agents/core_logic/phase_transition_detector.py
New-Item -ItemType File -Path src/thermo_agents/core_logic/record_range_builder.py
New-Item -ItemType File -Path src/thermo_agents/core_logic/thermodynamic_engine.py
New-Item -ItemType File -Path src/thermo_agents/core_logic/reaction_engine.py
```

### Шаг 2: Портировать код из ноутбука

Для каждого модуля:

1. Скопировать соответствующую функцию из ноутбука
2. Обернуть в класс с инициализацией зависимостей
3. Добавить типизацию (type hints)
4. Добавить docstrings
5. Добавить логирование
6. Добавить обработку ошибок

### Шаг 3: Обновить `orchestrator.py`

1. Добавить импорты новых модулей
2. Инициализировать компоненты в `__init__()`
3. Переписать `process_query()` с использованием `ReactionEngine`

### Шаг 4: Тестирование

```powershell
# Запустить базовый тест
uv run main.py --test

# Ожидаемый результат:
# ✅ Расчет реакции выполнен
# Таблица с ΔH, ΔS, ΔG, K для диапазона 298-2500K
```

### Шаг 5: Коммит изменений

```powershell
git checkout -b refactor/stage-02-core-logic
git add src/thermo_agents/core_logic/
git add src/thermo_agents/orchestrator.py
git commit -m "feat(stage-02): Внедрение core-логики из calc_example.ipynb"
```

---

## Критерии завершения этапа

### ✅ Обязательные:

1. Созданы все 5 модулей в `core_logic/`
2. Код портирован из ноутбука с сохранением логики
3. Добавлено полное логирование всех этапов
4. `orchestrator.py` интегрирован с новыми модулями
5. `uv run main.py --test` выполняет расчет реакции Al₂O₃ + 3C → 2Al + 3CO
6. Результат включает таблицу с колонками: T, ΔH, ΔS, ΔG, ln(K), K
7. Логи сохраняются в `logs/sessions/` в том же формате

### ✅ Опциональные:

1. Добавлены unit-тесты для каждого модуля
2. Добавлена документация в `docs/`
3. Проверена производительность (сравнение с ноутбуком)

---

## Риски и ограничения

### ⚠️ Риски:

1. **Различия в данных:** Возможны расхождения в результатах из-за округлений или разных версий БД
2. **Производительность:** Численное интегрирование может быть медленным для больших диапазонов
3. **Отсутствие данных:** Не все вещества имеют полные записи в БД

### 🛡️ Митигация:

1. **Валидация результатов:** Сравнение с результатами из ноутбука на тестовых данных
2. **Оптимизация:** Кэширование расчетов для повторяющихся температур
3. **Обработка ошибок:** Понятные сообщения при отсутствии данных

### ⏪ План отката:

```powershell
git reset --hard backup-before-stage-02
git checkout dev_two
```

---

## Следующий этап

**Этап 3:** Объединение с форматированием и финальный вывод

- Создание единого форматтера для вывода таблиц 298-2500K
- Добавление информации об отобранных записях
- Красивое форматирование в стиле текущего вывода (Unicode символы, таблицы)

---

## Примечания

1. **Сохранение фиксированного диапазона:** На этом этапе всегда используем 298-2500K, независимо от запроса пользователя
2. **Шаг 100K:** Фиксированный шаг температуры (как в ноутбуке)
3. **Только reaction_calculation:** Обработка `compound_data` запросов откладывается
4. **Совместимость с логами:** Формат логов должен быть совместим с текущими сессионными логами

---

**Автор:** GitHub Copilot  
**Дата:** 2025-11-02  
**Версия:** 1.0
