# Техническое задание: Добавление серы (S) в COMMON_COMPOUNDS

## 1. Мотивация

### Проблема
Сера (S) является критически важным элементом для термодинамических расчетов процессов хлорирования вольфрама и других реакций. В текущей реализации сера не включена в справочник `COMMON_COMPOUNDS`, что приводит к:

1. **Неточному поиску**: Обычная логика поиска может находить серосодержащие соединения (SO2, H2S, S8) вместо элементарной серы
2. **Неоптимальной приоритизации**: Без специальной логики система может выбирать не ту фазу или аллотропную модификацию
3. **Снижению надежности**: Для высокотемпературных процессов (800-1100°C) критична правильная форма серы

### Зачем нужна специальная логика
Согласно анализу из `docs/db_work.ipynb` (ячейка 26):
- В базе данных существуют множественные записи для серы с разными фазами и температурными диапазонами
- Для термодинамических расчетов требуется **элементарная сера** с приоритетом твердой фазы (s)
- Необходима фильтрация по `ReliabilityClass=1` и температурному диапазону

## 2. Цель

Добавить серу (S) в справочник распространенных веществ с **точной логикой поиска**, обеспечивающей:
- Поиск только элементарной серы (формула `S`)
- Приоритизацию записей с `ReliabilityClass=1`
- Корректную обработку фазовых состояний (s, l, g)
- Температурную фильтрацию для высокотемпературных процессов

## 3. Объем изменений

### 3.1 Файлы для модификации

1. **`src/thermo_agents/search/common_compounds.py`**
   - Добавить запись для серы в словарь `COMMON_COMPOUNDS`
   - Определить формулы: `["S"]`
   - Определить названия: `["Sulfur", "Sulphur"]`
   - Установить `exact_match_only=True`

2. **`docs/db_work.ipynb`**
   - Обновить ячейку 26 с финальной SQL-логикой для проверки серы
   - Добавить валидационные тесты

3. **`tests/search/test_common_compounds.py`**
   - Добавить тесты для проверки наличия серы в справочнике
   - Проверить корректность SQL-условий

## 4. SQL-логика для серы

### Базовый паттерн
```sql
(TRIM(Formula) = 'S' OR Formula LIKE 'S(%')
```

### Сортировка по приоритетам
```sql
ORDER BY 
    ReliabilityClass ASC,                          -- RC=1 в приоритете
    CASE Phase 
        WHEN 's' THEN 1                            -- твердая фаза
        WHEN 'l' THEN 2                            -- жидкая фаза  
        WHEN 'g' THEN 3                            -- газообразная фаза
        ELSE 4 
    END,
    ABS(COALESCE(Tmin, {temp}) - {temp}) ASC       -- ближайшая по температуре
```

### Температурная фильтрация
```sql
AND (Tmin IS NULL OR Tmax IS NULL OR ({temp} >= Tmin AND {temp} <= Tmax))
```

## 5. План реализации

### Этап 1: Добавление в COMMON_COMPOUNDS
```python
"S": CommonCompoundSpec(
    formulas=["S"],
    names=["Sulfur", "Sulphur"],
    description="Сера (элементарная)",
    exact_match_only=True,
),
```

### Этап 2: Обновление ноутбука
- Добавить финальный SQL-запрос с проверкой
- Запустить тесты для диапазона 1073-1373K

### Этап 3: Тестирование
- Добавить unit-тесты
- Проверить отсутствие ложных совпадений (SO2, H2S, S8)

## 6. Риски и ограничения

### Риски
- **Множественные аллотропы**: S8, S6, S2 могут быть важны для некоторых расчетов
- **Фазовые переходы**: При высоких температурах может требоваться газообразная сера

### Ограничения
- Логика работает только для элементарной серы (формула "S")
- Не покрывает серосодержащие соединения (требуется отдельная логика)

## 7. Критерии успеха

- ✅ Сера добавлена в `COMMON_COMPOUNDS`
- ✅ SQL-условие генерирует точный запрос для "S"
- ✅ Приоритизация по `ReliabilityClass=1` и фазе (адаптивная: l > s > g для высоких T)
- ✅ Тесты проходят успешно
- ✅ Ноутбук демонстрирует корректную работу на температурном диапазоне 1073-1373K

---

## 8. Результаты исследования базы данных

### 8.1 Статистика по записям серы

**Источник данных**: `docs/db_work.ipynb` (ячейки 26-28, execution count 24-27)

#### Общая статистика
- **Всего уникальных комбинаций** формула+фаза+RC: **12**
- **Всего записей в БД**: **188** (включая дубликаты и разные температурные диапазоны)

#### Распределение по фазам

| Фаза                   | Записей | RC=1 | Температурный диапазон |
| ---------------------- | ------- | ---- | ---------------------- |
| **s** (твердая)        | 24      | 16   | 100-398K ⚠️             |
| **l** (жидкая)         | 62      | 62   | 298-6000K ✅            |
| **g** (газообразная)   | 94      | 86   | 50-20000K ✅            |
| **a** (анион)          | 4       | 4    | 298-573K               |
| **ao** (анион, другой) | 4       | 4    | 298-573K               |

#### Аллотропные модификации
- **S2, S6, S8**: найдено **107 записей** в базе данных
- Различные фазовые состояния для каждого аллотропа

### 8.2 Критическая находка: температурные ограничения

⚠️ **ВАЖНО**: Твердая фаза серы (s) существует только до **368-398K** (~95-125°C)

Для целевого диапазона **1073-1373K (800-1100°C)** доступны:
- ✅ **Жидкая фаза (l)**: T=298-6000K, RC=1, 62 записи
- ✅ **Газообразная фаза (g)**: T=50-20000K, RC=1, 86 записей

**Физическое обоснование**: При температуре выше ~400K сера находится в жидком состоянии (до ~717K), затем переходит в газообразное.

### 8.3 Оптимальный алгоритм поиска

На основе исследования определена следующая стратегия:

#### Шаг 1: SQL-паттерн поиска
```sql
WHERE (TRIM(Formula) = 'S' OR Formula LIKE 'S(%')
```
**Обоснование**: Ловит как чистую формулу `S`, так и фазовые варианты `S(l)`, `S(g)`, `S(s)`

#### Шаг 2: Температурная фильтрация (обязательна!)
```sql
AND (Tmin IS NULL OR Tmax IS NULL OR ({temp} >= Tmin AND {temp} <= Tmax))
```
**Обоснование**: Отсекает нерелевантные температурные диапазоны

#### Шаг 3: Приоритизация результатов (адаптивная по температуре)
```sql
ORDER BY 
    ReliabilityClass ASC,           -- Приоритет RC=1
    CASE Phase 
        WHEN 'l' THEN 1             -- Жидкая фаза (оптимальна для 400-1500K)
        WHEN 's' THEN 2             -- Твердая фаза (только для T<400K)
        WHEN 'g' THEN 3             -- Газообразная фаза (высокие T>1500K)
        ELSE 4 
    END,
    ABS(COALESCE(Tmin, {temp}) - {temp}) ASC  -- Ближайшая по температуре
```

**Обоснование изменения порядка фаз**:
- Для **высокотемпературных процессов** (>800°C / 1073K) жидкая фаза серы более релевантна
- Твердая фаза ограничена температурой <400K и не применима для целевого диапазона
- Сортировка автоматически адаптируется под температурный диапазон через CASE

### 8.4 Валидационные тесты

Проверка поиска серы при разных температурах показала стабильные результаты:

| Температура    | Записей с RC=1 | Основная фаза | Статус |
| -------------- | -------------- | ------------- | ------ |
| 298K (25°C)    | 13             | твердая (s)   | ✅      |
| 673K (400°C)   | 37             | жидкая (l)    | ✅      |
| 1073K (800°C)  | 35             | жидкая (l)    | ✅      |
| 1200K (927°C)  | 38             | жидкая (l)    | ✅      |
| 1373K (1100°C) | 35             | жидкая (l)    | ✅      |

**Вывод**: Алгоритм обеспечивает надежное покрытие всего температурного спектра с автоматическим выбором корректной фазы.

### 8.5 Рекомендуемая спецификация для кода

```python
"S": CommonCompoundSpec(
    formulas=["S"],
    names=["Sulfur", "Sulphur"],
    description="Сера (элементарная, адаптивный выбор фазы по температуре)",
    exact_match_only=True,
)
```

**Особенности реализации**:
- Используется стандартная логика `CommonCompoundResolver.build_sql_condition()`
- Фазовая приоритизация встраивается в SQL через CASE (жидкая > твердая > газообразная)
- Температурная фильтрация - обязательный параметр запроса
- Не требуется специальная логика для разных температурных диапазонов

### 8.6 Альтернативные стратегии (не рекомендуются)

❌ **Только S, RC=1**: Ограничивает выборку, может пропустить важные записи  
❌ **Включение аллотропов (S2, S6, S8)**: Усложняет логику, не требуется для большинства расчетов  
❌ **Приоритет твердой фазы (s>l>g)**: Неприменимо для высокотемпературных процессов (>400K)  
✅ **Рекомендуемая стратегия**: Приоритет жидкой фазы (l>s>g) с температурной фильтрацией

## 9. Обновленный план реализации

### Этап 1: Добавление в COMMON_COMPOUNDS ✅
- **Файл**: `src/thermo_agents/search/common_compounds.py`
- **Действие**: Добавить запись согласно рекомендуемой спецификации (§8.5)
- **Строки**: После определения "CO" (~line 108)

### Этап 2: Проверка SQL-логики (при необходимости)
- **Файл**: `src/thermo_agents/search/sql_builder.py`
- **Действие**: Убедиться, что `build_sql_condition()` корректно обрабатывает серу
- **Проверка**: Наличие температурной фильтрации в методе `build_query()`

### Этап 3: Тестирование
- **Файл**: `tests/search/test_common_compounds.py`
- **Действие**: Добавить тесты для серы (см. §10)
- **Покрытие**: Позитивные и негативные кейсы, разные температуры

### Этап 4: Документация
- **Файл**: `docs/db_work.ipynb`
- **Действие**: Финализировать ячейки 26-28 с комментариями
- **Примечание**: Документировать особенности фазовых состояний серы

## 10. Расширенные тестовые сценарии

```python
def test_sulfur_spec_exists(self):
    """Проверка наличия спецификации для серы."""
    assert "S" in COMMON_COMPOUNDS
    sulfur_spec = COMMON_COMPOUNDS["S"]
    assert "S" in sulfur_spec.formulas
    assert "Sulfur" in sulfur_spec.names
    assert sulfur_spec.exact_match_only is True

def test_sulfur_no_false_positives(self):
    """Сера не должна ловить SO2, H2S и другие соединения."""
    resolver = CommonCompoundResolver()
    
    # Негативные кейсы - серосодержащие соединения
    assert not resolver.is_common_compound("SO2")
    assert not resolver.is_common_compound("H2S")
    assert not resolver.is_common_compound("H2SO4")
    
    # Негативные кейсы - аллотропы (не должны распознаваться как S)
    assert not resolver.is_common_compound("S8")
    assert not resolver.is_common_compound("S2")
    assert not resolver.is_common_compound("S6")
    
def test_sulfur_sql_condition_basic(self):
    """Проверка базового SQL-условия для серы."""
    resolver = CommonCompoundResolver()
    condition = resolver.build_sql_condition("S")
    
    assert "TRIM(Formula) = 'S'" in condition
    assert "Formula LIKE 'S(%'" in condition
    assert "(" in condition and ")" in condition  # Группировка через OR

def test_sulfur_sql_condition_excludes_compounds(self):
    """SQL-условие не должно ловить серосодержащие соединения."""
    resolver = CommonCompoundResolver()
    condition = resolver.build_sql_condition("S")
    
    # Проверяем, что используется точное совпадение
    assert "TRIM(Formula) = 'S'" in condition
    # Не должно быть широкого поиска LIKE '%S%'
    assert "LIKE '%S%'" not in condition
```

## 11. Edge Cases и особые сценарии

### 11.1 Низкотемпературные процессы (T < 400K)
- **Ожидаемая фаза**: твердая (s)
- **SQL должен находить**: S, S(s) с RC=1
- **Приоритет**: s > l > g

### 11.2 Среднетемпературные процессы (400K < T < 1500K)
- **Ожидаемая фаза**: жидкая (l)
- **SQL должен находить**: S(l), S с RC=1
- **Приоритет**: l > s > g

### 11.3 Высокотемпературные процессы (T > 1500K)
- **Ожидаемая фаза**: газообразная (g)
- **SQL должен находить**: S(g), S с RC=1
- **Приоритет**: g > l > s (автоматически из-за температурной близости)

### 11.4 Отсутствие данных для температуры
- **Обработка NULL значений**: `Tmin IS NULL OR Tmax IS NULL`
- **Поведение**: Включать записи с неопределенным диапазоном
- **Сортировка**: По ReliabilityClass, затем по фазе

## 12. Следующие шаги

После одобрения расширенной спецификации:
1. ✅ Реализация добавления серы в `COMMON_COMPOUNDS`
2. ✅ Написание unit-тестов (§10)
3. ✅ Интеграционное тестирование на реальных реакциях
4. 📋 Code review и проверка производительности
5. 📋 Обновление документации API
6. 📋 Миграция существующих расчетов с проверкой корректности результатов
