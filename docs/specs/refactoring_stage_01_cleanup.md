# Этап 1: Удаление избыточной логики после LLM response

**Дата создания:** 2025-11-02  
**Статус:** Не начат  
**Приоритет:** Высокий  
**Зависимости:** Нет  

---

## Цель этапа

Упростить кодовую базу, удалив избыточную и перегруженную логику фильтрации, агрегации и расчетов, которая выполняется после получения ответа от LLM (`Raw response (JSON)`). Оставить только парсинг ответа LLM и минимальную валидацию извлеченных параметров.

---

## Мотивация

**Проблемы текущей реализации:**

1. **Избыточная сложность:** Многоуровневая система фильтрации (`FilterPipeline`, `filter_stages.py`) с 5+ стадиями обработки данных
2. **Дублирование логики:** Расчеты термодинамики разбросаны между `ThermodynamicCalculator`, `ReactionCalculator`, `MultiPhaseReactionCalculator`
3. **Неясная ответственность:** Перемешивание логики фильтрации, расчетов и форматирования
4. **Сложность поддержки:** Трудно понять, где именно происходит отбор записей из БД
5. **Громоздкое логирование:** Избыточные логи на каждом шаге фильтрации

**Преимущества после рефакторинга:**

- Чистая архитектура: LLM → Парсинг → (далее будет новая логика)
- Упрощенная отладка и тестирование
- Подготовка к внедрению проверенной логики из ноутбука

---

## Компоненты для удаления

### 1. Модуль фильтрации (`src/thermo_agents/filtering/`)

**Файлы для удаления:**

```
src/thermo_agents/filtering/
├── filter_pipeline.py              # ← Удалить (основной pipeline)
├── filter_stages.py                # ← Удалить (5 стадий фильтрации)
├── complex_search_stage.py         # ← Удалить
├── phase_segment_stage.py          # ← Удалить
├── reaction_validation_stage.py    # ← Удалить
├── reaction_validator.py           # ← Удалить
├── record_selector.py              # ← Удалить
├── phase_resolver.py               # ← Оставить (может понадобиться позже)
├── phase_segment_builder.py        # ← Оставить (используется в новой логике)
├── precomputed_data.py             # ← Оставить
└── constants.py                    # ← Оставить
```

**Причины удаления:**

- `filter_pipeline.py`: Сложная логика с 5 стадиями (дедупликация, фазовая фильтрация, валидация формул, приоритизация, финальная проверка)
- `filter_stages.py`: Реализация стадий с большим количеством конфигурации
- Остальные файлы: Вспомогательные компоненты для избыточной фильтрации

### 2. Устаревшие расчетные модули (`src/thermo_agents/calculations/`)

**Файлы для удаления:**

```
src/thermo_agents/calculations/
├── reaction_calculator.py          # ← Удалить (старая логика расчетов)
├── phase_transition_calculator.py  # ← Удалить (будет заменена)
├── record_transition_manager.py    # ← Удалить
├── transition_data_manager.py      # ← Удалить
├── performance_optimizer.py        # ← Удалить
└── thermodynamic_calculator.py     # ← Оставить частично (базовые формулы)
```

**Причины удаления:**

- Эти модули реализуют старую логику расчетов с многочисленными промежуточными этапами
- Логика из ноутбука `calc_example.ipynb` проще и понятнее
- `thermodynamic_calculator.py` оставляем частично — из него возьмем только формулы расчета Cp, H, S, G

### 3. Агрегация данных (`src/thermo_agents/aggregation/`)

**Удалить всю директорию:**

```
src/thermo_agents/aggregation/
└── (все файлы)  # ← Удалить полностью
```

**Причина:** Промежуточный слой агрегации не нужен при новой логике.

### 4. Метрики (`src/thermo_agents/metrics/`)

**Удалить всю директорию:**

```
src/thermo_agents/metrics/
└── (все файлы)  # ← Удалить полностью
```

**Причина:** Метрики производительности избыточны на данном этапе. Можно вернуть позже, если понадобится.

### 5. Валидация (`src/thermo_agents/validation/`)

**Удалить всю директорию:**

```
src/thermo_agents/validation/
└── (все файлы)  # ← Удалить полностью
```

**Причина:** Валидация будет упрощена и встроена в новую логику.

### 6. Модели данных (`src/thermo_agents/models/`)

**Оставить:**

```
src/thermo_agents/models/
├── extraction.py       # ← Оставить (ExtractedReactionParameters)
├── search.py           # ← Оставить (DatabaseRecord, SearchResult)
├── aggregation.py      # ← Удалить или оставить частично
├── static_data.py      # ← Оставить (YAMLCompoundData, YAMLPhaseRecord)
└── formatting.py       # ← Оставить
```

**Причина:** Базовые модели данных нужны для парсинга и YAML-кэша.

---

## Компоненты для сохранения

### 1. Поиск в БД (`src/thermo_agents/search/`)

**Оставить все файлы:**

```
src/thermo_agents/search/
├── database_connector.py   # ← Оставить (подключение к SQLite)
├── sql_builder.py          # ← Оставить (генерация SQL запросов)
├── compound_searcher.py    # ← Оставить (координация поиска)
└── __init__.py
```

**Причина:** Эти модули нужны для поиска данных в БД на следующих этапах.

**Примечание для Этапа 2:** `compound_searcher.py` будет расширен интеграцией с `StaticDataManager` для приоритетного использования YAML-кэша.

### 2. YAML-кэш распространенных веществ (`src/thermo_agents/storage/`)

**КРИТИЧНО: СОХРАНИТЬ И ИСПОЛЬЗОВАТЬ**

```
src/thermo_agents/storage/
├── static_data_manager.py   # ← ОСТАВИТЬ (критичен для производительности)
└── __init__.py
```

**YAML-файлы в data/static_compounds/:**

```
data/static_compounds/
├── H2O.yaml    # Вода
├── CO2.yaml    # Углекислый газ
├── CO.yaml     # Угарный газ
├── O2.yaml     # Кислород
├── NH3.yaml    # Аммиак
├── Cl2.yaml    # Хлор
├── HCl.yaml    # Хлороводород
├── NaCl.yaml   # Хлорид натрия
├── FeO.yaml    # Оксид железа
└── C.yaml      # Углерод (графит)
```

**Назначение StaticDataManager:**

- ⚡ **Быстрый доступ** к данным 10+ распространенных веществ без обращения к БД
- 📊 **Предвычисленные фазовые переходы** (точки плавления, кипения, сублимации)
- 🔄 **Кэширование в памяти** для повторных обращений (Dict cache)
- ✅ **Валидация через Pydantic** (YAMLCompoundData, YAMLPhaseRecord)
- 🕐 **Проверка актуальности** данных (timestamp в metadata)

**Структура YAML-файла (пример H2O.yaml):**

```yaml
compound:
  formula: "H2O"
  common_names: ["Water", "Вода"]
  description: "Water - most common chemical compound"
  
  phases:
    - phase: "s"          # Твердая (лед)
      tmin: 3.0
      tmax: 273.151
      h298: -285.830002
      s298: 69.949995
      f1: 30.092          # Коэффициенты Шомейта
      f2: 6.832
      # ... f3-f6
      tmelt: 273.15
      tboil: 373.15
      
    - phase: "l"          # Жидкая
      tmin: 273.151
      tmax: 495.0
      # ...
    
    - phase: "g"          # Газообразная
      tmin: 298.15
      tmax: 6000.0
      # ...
  
  metadata:
    extracted_date: "2025-11-01"
    source: "Glushko 94, Barin 93"
    reliability_class: 1
```

**Интеграция на Этапе 2:**

```python
# Псевдокод для этапа 2: CompoundDataLoader
class CompoundDataLoader:
    def __init__(self, db_connector, static_manager):
        self.db_connector = db_connector
        self.static_manager = static_manager  # ← StaticDataManager
    
    def get_raw_compound_data(self, formula: str, names: List[str]) -> pd.DataFrame:
        # Шаг 1: Проверить YAML-кэш (приоритет!)
        if self.static_manager.is_available(formula):
            yaml_data = self.static_manager.load_compound(formula)
            return self._convert_yaml_to_dataframe(yaml_data)
        
        # Шаг 2: Двухстадийный поиск в БД
        # (формула + имя) → (только формула)
        return self._database_search(formula, names)
```

**Преимущества:**

- 🚀 **Производительность**: Мгновенный доступ для H2O, CO2, O2, NH3, etc.
- 🎯 **Надежность**: Проверенные данные с указанием источников (Glushko, Barin)
- 💾 **Кэширование**: Повторные запросы не требуют даже файловых операций
- 🔍 **Отладка**: Легко проверить и изменить данные в YAML

**⚠️ НЕ УДАЛЯТЬ** — этот компонент:
- Критичен для производительности (90%+ запросов для распространенных веществ)
- Уже интегрирован в текущий MultiPhaseOrchestrator
- Содержит проверенные данные для 10 веществ
- Будет расширен на следующих этапах

---

## Изменения в `orchestrator.py`

**Файл:** `src/thermo_agents/orchestrator.py`

### Удалить методы:

1. `_process_reaction_calculation_multi_phase()` — строки ~320-420
2. `_build_multi_phase_data()` — строки ~290-320
3. `_determine_full_calculation_range()` — строки ~260-290
4. `process_query_with_multi_phase()` — строки ~200-260
5. Все обращения к `self.filter_pipeline`
6. Все обращения к `self.reaction_calculator` (старый)

### Оставить:

1. `__init__()` — инициализация компонентов
2. `process_query()` — основной метод (будет упрощен)
3. Парсинг LLM response через `self.thermodynamic_agent.extract_parameters()`
4. Базовую структуру класса

### Упрощенная структура `process_query()`:

```python
async def process_query(self, user_query: str) -> str:
    """
    Обработка запроса пользователя.
    
    ЭТАП 1: Только парсинг LLM response, без дальнейшей обработки.
    """
    try:
        # 1. Логирование запроса
        if self.session_logger:
            self.session_logger.log_llm_request(user_query)
        
        # 2. Извлечение параметров через LLM
        if not self.thermodynamic_agent:
            return "❌ LLM агент не инициализирован"
        
        params = await self.thermodynamic_agent.extract_parameters(user_query)
        
        # 3. Логирование ответа LLM
        if self.session_logger:
            self.session_logger.log_llm_response(params)
        
        # 4. ВРЕМЕННАЯ ЗАГЛУШКА (будет заменена на этапе 2)
        return f"✅ Параметры извлечены:\n{params.model_dump_json(indent=2)}\n\n⚠️ Расчеты временно недоступны (ожидается этап 2)"
    
    except Exception as e:
        self.logger.error(f"Ошибка обработки запроса: {e}")
        return f"❌ Ошибка: {str(e)}"
```

---

## Изменения в `thermodynamic_agent.py`

**Оставить без изменений:**

- Этот файл отвечает только за извлечение параметров из текста через LLM
- Его логика корректна и не требует изменений на данном этапе

---

## Изменения в моделях данных

**Директория:** `src/thermo_agents/models/`

### Удалить модели:

```
src/thermo_agents/models/
├── aggregation.py          # ← Удалить (MultiPhaseReactionData и др.)
├── search.py               # ← Оставить частично, удалить MultiPhaseSearchResult
└── extraction.py           # ← Оставить (ExtractedReactionParameters)
```

**Причина:** Модели агрегации больше не нужны. Оставляем только базовые модели для парсинга LLM response.

---

## Изменения в форматировании

**Директория:** `src/thermo_agents/formatting/`

**Временно оставить, но не использовать:**

- `compound_data_formatter.py`
- `reaction_calculation_formatter.py`

На этапе 3 эти форматтеры будут переработаны под новый формат вывода.

---

## План миграции

### Шаг 1: Создать резервную копию

```powershell
# Создать ветку для рефакторинга
git checkout -b refactor/stage-01-cleanup

# Создать backup текущей версии
git tag backup-before-stage-01
```

### Шаг 2: Удалить файлы

```powershell
# Удаление директорий
Remove-Item -Recurse src/thermo_agents/aggregation
Remove-Item -Recurse src/thermo_agents/metrics
Remove-Item -Recurse src/thermo_agents/validation

# Удаление файлов фильтрации
Remove-Item src/thermo_agents/filtering/filter_pipeline.py
Remove-Item src/thermo_agents/filtering/filter_stages.py
Remove-Item src/thermo_agents/filtering/complex_search_stage.py
Remove-Item src/thermo_agents/filtering/phase_segment_stage.py
Remove-Item src/thermo_agents/filtering/reaction_validation_stage.py
Remove-Item src/thermo_agents/filtering/reaction_validator.py
Remove-Item src/thermo_agents/filtering/record_selector.py

# Удаление расчетных модулей
Remove-Item src/thermo_agents/calculations/reaction_calculator.py
Remove-Item src/thermo_agents/calculations/phase_transition_calculator.py
Remove-Item src/thermo_agents/calculations/record_transition_manager.py
Remove-Item src/thermo_agents/calculations/transition_data_manager.py
Remove-Item src/thermo_agents/calculations/performance_optimizer.py
```

### Шаг 3: Упростить `orchestrator.py`

- Удалить импорты удаленных модулей
- Удалить методы многофазных расчетов
- Упростить `process_query()` до заглушки (см. выше)

### Шаг 4: Обновить `__init__.py` файлы

Удалить импорты несуществующих модулей из:

- `src/thermo_agents/__init__.py`
- `src/thermo_agents/filtering/__init__.py`
- `src/thermo_agents/calculations/__init__.py`

### Шаг 5: Проверить работоспособность

```powershell
# Запустить базовый тест (должен вернуть заглушку с извлеченными параметрами)
uv run main.py --test

# Ожидаемый результат:
# ✅ Параметры извлечены: {...}
# ⚠️ Расчеты временно недоступны (ожидается этап 2)
```

---

## Критерии завершения этапа

### ✅ Обязательные:

1. Удалены все указанные файлы и директории
2. `orchestrator.py` упрощен до парсинга LLM response
3. Проект запускается без ошибок импорта
4. `uv run main.py --test` возвращает корректную заглушку
5. Все изменения закоммичены в ветку `refactor/stage-01-cleanup`

### ✅ Опциональные:

1. Обновлена документация `docs/ARCHITECTURE.md`
2. Удалены соответствующие тесты для удаленных модулей

---

## Риски и ограничения

### ⚠️ Риски:

1. **Временная недоступность расчетов:** После этапа 1 система не сможет выполнять расчеты (только парсинг)
2. **Поломка существующих тестов:** Многие интеграционные тесты перестанут работать
3. **Зависимости в других модулях:** Возможны скрытые зависимости от удаляемых компонентов

### 🛡️ Митигация:

1. **Резервная копия:** Создан git tag `backup-before-stage-01`
2. **Поэтапный подход:** Этап 1 сознательно ограничен удалением, без добавления новой логики
3. **Тестирование:** Базовая проверка работоспособности после каждого шага

### ⏪ План отката:

```powershell
# Откат к предыдущему состоянию
git reset --hard backup-before-stage-01
git checkout dev_two
```

---

## Следующий этап

**Этап 2:** Внедрение логики из ноутбука `calc_example.ipynb` с логированием

- Создание новых модулей на основе функций из ноутбука
- Интеграция с `orchestrator.py`
- Добавление логирования в стиле текущей системы

---

## Примечания

1. **Не трогаем `search/` модуль:** Поиск в БД работает корректно, его логика остается без изменений
2. **Не трогаем `storage/`:** StaticDataManager и YAML кэш остаются
3. **Не трогаем `temperature/`:** Модуль работы с температурными диапазонами может понадобиться
4. **Сохраняем `session_logger.py`:** Логирование будет использоваться на этапе 2

---

**Автор:** GitHub Copilot  
**Дата:** 2025-11-02  
**Версия:** 1.0
