# Техническое задание на рефакторинг системы термодинамических агентов

**Дата:** 15 октября 2025  
**Версия:** 1.0  
**Уровень уверенности:** 85/100

## Цель рефакторинга

Упростить архитектуру проекта путём замены агентов на базе LLM детерминированной логикой там, где это возможно. Сохранить использование LLM только для извлечения параметров из естественного языка.

## Принципиальные изменения

### Что остаётся (LLM)
- **Thermodynamic Agent** — извлечение параметров из запросов на естественном языке

### Что упраздняется (замена на детерминированную логику)
- **SQL Generation Agent** — заменяется модулем генерации SQL по правилам
- **Results Filtering Agent** — заменяется модульной системой фильтрации
- **Database Agent** — частично упраздняется, логика переносится в новый модуль

### Что добавляется
- **Deterministic Search Module** — модульная система поиска и фильтрации записей
- **Temperature Range Resolver** — модуль расчёта температурных диапазонов и фазовых переходов
- **Reaction Data Aggregator** — модуль агрегации данных по всем веществам реакции

---

## Новый поток выполнения

### Шаг 1: Извлечение параметров (LLM)
**Агент:** Thermodynamic Agent  
**Вход:** Запрос пользователя на естественном языке  
**Выход:** `ExtractedReactionParameters`

```python
class ExtractedReactionParameters(BaseModel):
    balanced_equation: str                    # "TiO2 + 2HCl → TiCl4 + H2O"
    all_compounds: List[str]                  # ["TiO2", "HCl", "TiCl4", "H2O"] (до 10 веществ)
    reactants: List[str]                      # ["TiO2", "HCl"]
    products: List[str]                       # ["TiCl4", "H2O"]
    temperature_range_k: Tuple[float, float]  # (298, 673)
    extraction_confidence: float              # 0.0-1.0
    missing_fields: List[str]                 # ["temperature_range"] если не извлечено
    
    # Примечание: стехиометрические коэффициенты НЕ используются в логике поиска
```

**Критерий успеха:** Все обязательные поля заполнены (`balanced_equation`, `reactants`, `products`, `temperature_range_k`)

**Обработка ошибок:** Если `missing_fields` не пуст, вернуть пользователю сообщение с просьбой уточнить запрос.

---

### Шаг 2: Поиск и фильтрация для каждого вещества (Детерминированная логика)
**Модуль:** Deterministic Search Module  
**Вход:** Одно химическое вещество + температурный диапазон  
**Выход:** `CompoundSearchResult`

```python
class CompoundSearchResult(BaseModel):
    compound_formula: str
    records_found: List[DatabaseRecord]
    filter_statistics: FilterStatistics
    coverage_status: str  # "full", "partial", "none"
    warnings: List[str]
```

```python
class FilterStatistics(BaseModel):
    stage_1_initial_matches: int           # Стадия 1: Найдено по формуле
    stage_1_description: str               # "Поиск по формуле"
    
    stage_2_temperature_filtered: int      # Стадия 2: После температурной фильтрации
    stage_2_description: str               # "Температурный диапазон 298-673K"
    
    stage_3_phase_selected: int            # Стадия 3: После выбора фаз
    stage_3_description: str               # "Фазовый состав - газ при T>373K"
    
    stage_4_final_selected: int            # Стадия 4: Финальный набор
    stage_4_description: str               # "Приоритизация по надёжности"
    
    is_found: bool                         # True если хотя бы 1 запись выбрана
    failure_stage: Optional[int]           # На какой стадии поиск провалился (если is_found=False)
    failure_reason: Optional[str]          # Причина провала
```

#### Подэтапы фильтрации:

**2.1. SQL-поиск по формуле**
- Поиск по `TRIM(Formula) = '{compound}'`
- Поиск по `Formula LIKE '{compound}(%'` для фазовых/ионных вариантов
- Сортировка по `ReliabilityClass ASC`
- LIMIT 100

**2.2. Температурная фильтрация**
- Отсев записей вне температурного диапазона
- Логика пересечения интервалов `[Tmin, Tmax]` с исследуемым диапазоном
- Обработка NULL значений Tmin/Tmax

**2.3. Фазовая фильтрация с учётом фазовых переходов**
- Определение агрегатного состояния при заданной температуре
- Использование Tmelt, Tboil для определения фазы
- Приоритизация записей с корректной фазой

**2.4. Покрытие температурного диапазона**
- Проверка: покрывают ли найденные записи весь исследуемый диапазон
- Если нет — поиск дополнительных записей для заполнения пробелов
- Статус: `full` / `partial` / `none`

---

### Шаг 3: Агрегация результатов (Детерминированная логика)
**Модуль:** Reaction Data Aggregator  
**Вход:** Список `CompoundSearchResult` для всех веществ реакции  
**Выход:** `AggregatedReactionData`

```python
class AggregatedReactionData(BaseModel):
    reaction_equation: str
    compounds_data: List[CompoundSearchResult]
    summary_table_formatted: str       # Таблица свойств, отформатированная через tabulate
    completeness_status: str           # "complete", "partial", "incomplete"
    missing_compounds: List[str]       # Вещества без данных
    found_compounds: List[str]         # Вещества с успешным поиском
    detailed_statistics: Dict[str, FilterStatistics]  # Статистика по каждому веществу
    warnings: List[str]
    recommendations: List[str]         # Рекомендации пользователю при ошибках
```

**Формат сводной таблицы (tabulate):**

Порядок колонок строго соблюдается:
1. **Формула** — химическая формула вещества
2. **Фаза** — агрегатное состояние (s/l/g/aq)
3. **T_диапазон** — температурный диапазон применимости (K)
4. **H298** — энтальпия образования при 298K (кДж/моль)
5. **S298** — энтропия при 298K (Дж/моль·K)
6. **Cp_коэффициенты** — коэффициенты теплоемкости f1-f6
7. **Надёжность** — класс надёжности данных (1-4)

```python
from tabulate import tabulate

headers = ["Формула", "Фаза", "T_диапазон (K)", "H298 (кДж/моль)", 
           "S298 (Дж/моль·K)", "Cp_коэффициенты (f1-f6)", "Надёжность (класс)"]
table_data = [
    ["TiO2", "s", "298-2130", "-944.0", "50.6", "75.19,-0.00258,...", "1"],
    ["HCl", "g", "298-1800", "-92.3", "186.9", "29.13,-0.00013,...", "1"],
    # ...
]
formatted_table = tabulate(table_data, headers=headers, tablefmt="grid")
```

---

### Шаг 4: Возврат результата пользователю (Оркестратор)
**Агент:** Thermo Orchestrator  
**Вход:** `AggregatedReactionData`  
**Выход:** Форматированный текстовый ответ на русском языке

**Формат ответа:**
```
✅ Термодинамические данные для реакции:
   TiO2 + 2HCl → TiCl4 + H2O при 298-673K

📊 Найденные данные (tabulate):
┌──────────┬───────┬───────────┬──────────┬──────────┬──────────────────────┬──────────────────┐
│ Формула  │ Фаза  │ T_диапазон│ H298     │ S298     │ Cp_коэффициенты      │ Надёжность       │
│          │       │ (K)       │ (кДж/моль│ (Дж/моль │ (f1-f6)              │ (класс)          │
│          │       │           │ )        │ ·K)      │                      │                  │
├──────────┼───────┼───────────┼──────────┼──────────┼──────────────────────┼──────────────────┤
│ TiO2     │ s     │ 298-2130  │ -944.0   │ 50.6     │ 75.19,-0.00258,...   │ 1                │
│ HCl      │ g     │ 298-1800  │ -92.3    │ 186.9    │ 29.13,-0.00013,...   │ 1                │
│ TiCl4    │ l     │ 298-673   │ -804.2   │ 252.4    │ 145.2,0.0,...        │ 2                │
│ H2O      │ g     │ 298-2500  │ -241.8   │ 188.8    │ 30.09,0.00692,...    │ 1                │
└──────────┴───────┴───────────┴──────────┴──────────┴──────────────────────┴──────────────────┘

📈 Детальная статистика фильтрации:

TiO2:
  ├─ Стадия 1 (Поиск по формуле): найдено 15 записей
  ├─ Стадия 2 (Температурный диапазон 298-673K): осталось 8 записей
  ├─ Стадия 3 (Фазовый состав - твёрдое при T<2130K): осталось 3 записи
  └─ Стадия 4 (Приоритизация по надёжности): выбрана 1 запись

HCl:
  ├─ Стадия 1 (Поиск по формуле): найдено 23 записи
  ├─ Стадия 2 (Температурный диапазон 298-673K): осталось 12 записей
  ├─ Стадия 3 (Фазовый состав - газ при T>188K): осталось 8 записей
  └─ Стадия 4 (Приоритизация по надёжности): выбрана 1 запись

TiCl4:
  ├─ Стадия 1 (Поиск по формуле): найдено 8 записей
  ├─ Стадия 2 (Температурный диапазон 298-673K): осталось 3 записи
  ├─ Стадия 3 (Фазовый состав - жидкость при 298-673K): осталось 2 записи
  └─ Стадия 4 (Приоритизация по надёжности): выбрана 1 запись

H2O:
  ├─ Стадия 1 (Поиск по формуле): найдено 34 записи
  ├─ Стадия 2 (Температурный диапазон 298-673K): осталось 18 записей
  ├─ Стадия 3 (Фазовый состав - газ при T>373K): осталось 9 записей
  └─ Стадия 4 (Приоритизация по надёжности): выбрана 1 запись

⚠️ Предупреждения:
   - Для TiCl4 частичное покрытие: найдены данные только для 298-673K (не покрыт диапазон 673-900K)
   
❌ Ненайденные вещества:
   Все вещества успешно найдены в базе данных.
```

**Формат ответа при отсутствии данных:**
```
❌ Термодинамические данные для реакции:
   Fe2O3 + 3H2 → 2Fe + 3H2O при 1200-1500K

📈 Детальная статистика фильтрации:

Fe2O3:
  ├─ Стадия 1 (Поиск по формуле): найдено 12 записей
  ├─ Стадия 2 (Температурный диапазон 1200-1500K): осталось 2 записи
  ├─ Стадия 3 (Фазовый состав - твёрдое при T<1838K): осталось 1 запись
  └─ Стадия 4 (Приоритизация по надёжности): выбрана 1 запись

H2:
  ├─ Стадия 1 (Поиск по формуле): найдено 18 записей
  ├─ Стадия 2 (Температурный диапазон 1200-1500K): осталось 0 записей
  └─ ❌ ВЕЩЕСТВО НЕ НАЙДЕНО: нет данных в температурном диапазоне 1200-1500K

Fe:
  ├─ Стадия 1 (Поиск по формуле): найдено 0 записей
  └─ ❌ ВЕЩЕСТВО НЕ НАЙДЕНО: отсутствует в базе данных

H2O:
  ├─ Стадия 1 (Поиск по формуле): найдено 34 записи
  ├─ Стадия 2 (Температурный диапазон 1200-1500K): осталось 8 записей
  ├─ Стадия 3 (Фазовый состав - газ при T>373K): осталось 4 записи
  └─ Стадия 4 (Приоритизация по надёжности): выбрана 1 запись

❌ Критическая ошибка:
   Невозможно предоставить термодинамические данные для реакции.
   Отсутствуют данные для веществ: H2 (нет данных в диапазоне), Fe (не найдено в БД)
   
💡 Рекомендация:
   Попробуйте изменить температурный диапазон или уточните химические формулы веществ.
```

---

## Архитектура модулей

### Модульная структура фильтрации

```python
class FilterStage(Protocol):
    """Базовый протокол для стадии фильтрации."""
    def filter(self, records: List[Record], context: FilterContext) -> List[Record]:
        ...
    def get_statistics(self) -> Dict[str, int]:
        ...

class FilterPipeline:
    """Конвейер фильтрации с возможностью добавления новых стадий."""
    def __init__(self):
        self.stages: List[FilterStage] = []
    
    def add_stage(self, stage: FilterStage) -> None:
        self.stages.append(stage)
    
    def execute(self, records: List[Record], context: FilterContext) -> FilterResult:
        ...
```

**Встроенные стадии:**
1. `FormulaMatchStage` — поиск по формуле
2. `TemperatureFilterStage` — фильтрация по температуре
3. `PhaseSelectionStage` — выбор фаз с учётом переходов
4. `CoverageCheckStage` — проверка покрытия диапазона
5. `ReliabilityPriorityStage` — приоритизация по надёжности

---

## Структура файлов после рефакторинга

```
src/thermo_agents/
├── __init__.py
├── agent_storage.py                  # Сохраняется (для A2A если нужно)
├── thermodynamic_agent.py            # ✅ Сохраняется (LLM для NLP)
├── orchestrator.py                   # ✅ Сохраняется (координация)
│
├── search/                           # 🆕 Новый модуль поиска
│   ├── __init__.py
│   ├── deterministic_search.py       # Главный модуль поиска
│   ├── sql_builder.py                # Детерминированная генерация SQL
│   ├── database_connector.py         # Выполнение запросов к БД
│   └── compound_searcher.py          # Логика поиска одного вещества
│
├── filtering/                        # 🆕 Модульная фильтрация
│   ├── __init__.py
│   ├── filter_pipeline.py            # Конвейер фильтрации
│   ├── filter_stages.py              # Реализация стадий
│   ├── temperature_resolver.py       # Температурные диапазоны
│   └── phase_resolver.py             # Определение фаз
│
├── aggregation/                      # 🆕 Агрегация данных
│   ├── __init__.py
│   ├── reaction_aggregator.py        # Агрегация по реакции
│   └── table_formatter.py            # Форматирование таблиц
│
├── models/                           # 🆕 Pydantic модели
│   ├── __init__.py
│   ├── extraction.py                 # ExtractedReactionParameters
│   ├── search.py                     # CompoundSearchResult
│   └── aggregation.py                # AggregatedReactionData
│
├── prompts.py                        # Сохраняется (только для Thermo Agent)
├── thermo_agents_logger.py           # Сохраняется
└── operations.py                     # Сохраняется/обновляется

# ❌ Удаляются:
# - sql_generation_agent.py
# - database_agent.py  
# - results_filtering_agent.py
# - individual_search_agent.py (если есть)
```

---

## Этапы реализации

### Этап 0: Комплексное исследование базы данных (3-5 дней)
**КРИТИЧЕСКИ ВАЖНЫЙ ЭТАП** — без него невозможна разработка корректной стратегии фильтрации

#### Задачи исследования:
- [ ] **Анализ структуры данных**
  - Изучить схему таблицы `compounds` (все 32 поля)
  - Проанализировать распределение NULL значений по полям
  - Определить паттерны заполнения температурных диапазонов (Tmin, Tmax)
  - Изучить распределение по `ReliabilityClass` (1-4)

- [ ] **Анализ химических формул**
  - Паттерны записи формул: чистые (H2O), с фазами (H2O(g)), ионные (Fe+3)
  - Вариативность написания одного вещества (H2O, H2O(l), H2O(aq))
  - Дубликаты и их семантика (разные температурные диапазоны, разные источники)

- [ ] **Температурные характеристики**
  - Распределение температурных диапазонов [Tmin, Tmax]
  - Частота записей с NULL в Tmin/Tmax и их интерпретация
  - Анализ покрытия диапазонов для типичных веществ (H2O, O2, Fe, TiO2, NaCl)
  - Корреляция температурных диапазонов с фазовыми переходами (Tmelt, Tboil)

- [ ] **Фазовые состояния**
  - Распределение по фазам: s, l, g, aq
  - Как фаза закодирована (в Formula или отдельное поле Phase)
  - Наличие данных для фазовых переходов (Tmelt, Tboil, Tsublim)
  - % записей с заполненными Tmelt/Tboil

- [ ] **Термодинамические свойства**
  - Полнота заполнения: H298, S298, коэффициенты f1-f6
  - Паттерны отсутствия данных (какие свойства часто NULL)
  - Альтернативные представления (H0, G0, Cp298)

- [ ] **Тестовые запросы**
  - H2O: вода в разных фазах при 298-673K
  - TiO2 + 2HCl → TiCl4 + H2O: хлорирование оксида титана при 600-900K
  - Fe: железо с фазовыми переходами (s→l при 1811K)
  - O2: кислород (g) в широком диапазоне
  - NaCl: соль (s→l при 1074K, есть aq форма)

#### Результаты этапа:
- [ ] **Отчёт об исследовании** в `docs/database_analysis_report.md`:
  - Статистика по каждому типу анализа
  - Выявленные проблемы и ограничения БД
  - Рекомендации по стратегии поиска и фильтрации
  
- [ ] **Обоснованная стратегия фильтрации**:
  - Приоритеты полей для поиска
  - Логика обработки NULL значений
  - Алгоритм выбора между дубликатами
  - Правила покрытия температурных диапазонов
  - Логика определения фазы при заданной температуре

- [ ] **Jupyter Notebook** `docs/filtering_strategy_prototype.ipynb`:
  - Прототип каждой стадии фильтрации
  - Тестирование на реальных примерах
  - Метрики качества фильтрации

### Этап 1: Разработка детерминированной генерации SQL (2-3 дня)
- [ ] Создать `sql_builder.py` на основе результатов исследования
- [ ] Реализовать поиск по формуле с учётом вариативности написания
- [ ] Добавить сортировку по приоритетам (ReliabilityClass, полнота данных)
- [ ] Протестировать на примерах из Этапа 0

### Этап 2: Разработка модуля поиска (3-4 дня)
- [ ] Создать `search/` модуль
- [ ] Реализовать `CompoundSearcher` для одного вещества
- [ ] Реализовать `DatabaseConnector` для выполнения SQL
- [ ] Написать unit-тесты для каждого компонента

### Этап 3: Разработка модульной фильтрации (4-5 дней)
- [ ] Создать `filtering/` модуль с конвейерной архитектурой
- [ ] Реализовать `FilterPipeline` и стадии фильтрации **в порядке приоритета**:
  1. **FormulaMatchStage** — поиск по химической формуле
  2. **TemperatureFilterStage** — фильтрация по температурному диапазону
  3. **PhaseSelectionStage** — фильтрация по фазовому составу
  4. **ReliabilityPriorityStage** — все остальные критерии
- [ ] Реализовать `TemperatureResolver` для расчёта диапазонов
- [ ] Реализовать `PhaseResolver` для определения фаз с учётом Tmelt/Tboil
- [ ] Добавить **детальную статистику** на каждой стадии фильтрации
- [ ] Написать unit-тесты для фильтрации

### Этап 4: Агрегация и форматирование (2-3 дня)
- [ ] Создать `aggregation/` модуль
- [ ] Реализовать `ReactionAggregator` с поддержкой до 10 веществ
- [ ] Реализовать `TableFormatter` с использованием **tabulate**
- [ ] Добавить генерацию **детальной статистики фильтрации**:
  - Количество записей на каждой стадии для каждого вещества
  - Сообщения о ненайденных веществах
  - Предупреждения о частичном покрытии температурных диапазонов

### Этап 5: Обновление Thermodynamic Agent (1-2 дня)
- [ ] Обновить модель `ExtractedReactionParameters`
- [ ] Обновить промпт для извлечения уравненной реакции (до 10 веществ)
- [ ] Добавить валидацию обязательных полей
- [ ] Убрать зависимость от стехиометрических коэффициентов в логике поиска

### Этап 6: Рефакторинг Orchestrator (2-3 дня)
- [ ] Упростить логику координации
- [ ] Убрать зависимости от упразднённых агентов
- [ ] Интегрировать новые модули поиска и агрегации
- [ ] Обновить форматирование ответов с использованием **tabulate**
- [ ] Добавить вывод детальной статистики фильтрации

### Этап 7: Тестирование и документация (3-4 дня)
- [ ] Интеграционные тесты с реальными запросами (включая 10-компонентные реакции)
- [ ] Тестирование граничных случаев:
  - Вещества без данных в БД
  - Частичное покрытие температурных диапазонов
  - Фазовые переходы внутри исследуемого диапазона
- [ ] Обновить `CLAUDE.md` и `README.md`
- [ ] Создать примеры использования
- [ ] Документировать результаты исследования БД из Этапа 0

### Этап 8: Миграция и очистка (1-2 дня)
- [ ] Удалить упразднённые файлы агентов
- [ ] Обновить `main.py`
- [ ] Добавить `tabulate` в `pyproject.toml`
- [ ] Финальное тестирование

**Общая оценка времени:** 20-30 дней (с учётом критически важного Этапа 0)

---

## Риски и митигация

| Риск                                           | Вероятность | Влияние     | Митигация                                                                     |
| ---------------------------------------------- | ----------- | ----------- | ----------------------------------------------------------------------------- |
| Недостаточное исследование БД на Этапе 0       | Средняя     | Критическое | **Выделить 3-5 дней на детальный анализ, не переходить к Этапу 1 без отчёта** |
| Детерминированная логика не покроет все случаи | Средняя     | Высокое     | Тщательное тестирование на разнообразных веществах из Этапа 0                 |
| Сложность фазовых переходов                    | Высокая     | Среднее     | Прототипирование в Jupyter на Этапе 0, итеративная разработка                 |
| Неполные данные в БД                           | Средняя     | Среднее     | Детальные сообщения с указанием стадии провала и количества записей           |
| Регрессия функциональности                     | Низкая      | Высокое     | Сохранить старую версию на отдельной ветке, A/B тестирование                  |
| Производительность с 10 веществами             | Низкая      | Среднее     | Оптимизация SQL-запросов, кеширование результатов поиска                      |

---

## Критерии приёмки

### Обязательные критерии:
1. ✅ **Этап 0 выполнен полностью**: Создан отчёт об исследовании БД с обоснованием стратегии фильтрации
2. ✅ **Извлечение параметров**: Система корректно извлекает до 10 веществ из уравнения реакции
3. ✅ **Детерминированная фильтрация**: Соблюдается строгий порядок стадий:
   - Стадия 1: Поиск по химической формуле
   - Стадия 2: Фильтрация по температурному диапазону
   - Стадия 3: Фильтрация по фазовому составу
   - Стадия 4: Приоритизация по остальным критериям
4. ✅ **Фазовые переходы**: Корректный учёт Tmelt/Tboil при определении агрегатного состояния
5. ✅ **Детальная статистика**: Для каждого вещества выводится количество записей на каждой стадии
6. ✅ **Информативные сообщения**: При отсутствии данных указывается на какой стадии поиск провалился
7. ✅ **Форматирование с tabulate**: Все таблицы выводятся через библиотеку tabulate
8. ✅ **Модульность**: Возможность добавления новых стадий фильтрации без изменения существующих
9. ✅ **Производительность**: Обработка запроса с 10 веществами < 5 секунд
10. ✅ **Покрытие тестами**: > 80% для всех новых модулей

### Дополнительные критерии:
- ✅ Стехиометрические коэффициенты не влияют на логику поиска
- ✅ Поддержка реакций с до 10 компонентами
- ✅ Расширяемость для будущих функций (расчёты, визуализация)

---

## Следующие шаги

1. **Согласовать ТЗ** с командой/заказчиком
2. **Расширить детали** по каждому модулю (отдельные мини-ТЗ)
3. **Создать прототип** на 1-2 примерах для валидации подхода
4. **Начать разработку** по этапам

---

## Ответы на вопросы уточнения

1. **Формат вывода**: Использовать библиотеку `tabulate` для форматирования таблиц
2. **Обработка частичных данных**: Сообщать пользователю о ненайденных веществах с указанием количества записей на каждой стадии фильтрации
3. **Приоритеты фильтрации**: 
   - Сначала химическая формула
   - Затем температурный диапазон
   - Затем фазовый состав
   - Затем все остальные критерии
4. **Сложность реакций**: Поддержка до 10 веществ в реакции, стехиометрические коэффициенты не важны для логики поиска
5. **Будущие функции**: Планируется расширение функциональности (расчёты, визуализация)

---

**Подготовил:** GitHub Copilot  
**Статус:** Утверждено v1.1 — готово к реализации
