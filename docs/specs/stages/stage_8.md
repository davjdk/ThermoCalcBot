# Этап 8: Миграция и очистка

**Длительность:** 1-2 дня  
**Приоритет:** Средний  
**Статус:** Не начат  
**Зависимости:** Этап 7

---

## Описание

Финальная миграция кода, удаление устаревших компонентов и проверка работоспособности системы в production-режиме.

---

## Основные задачи

### 1. Удалить упразднённые файлы агентов

**Файлы для удаления:**

```
src/thermo_agents/
├── sql_generation_agent.py          ❌ Удалить
├── database_agent.py                 ❌ Удалить
├── results_filtering_agent.py        ❌ Удалить (если есть)
└── individual_search_agent.py        ❌ Удалить (если есть)
```

**Процедура:**
1. Проверить отсутствие зависимостей в коде
2. Сделать backup (commit на отдельную ветку)
3. Удалить файлы
4. Прогнать все тесты

**Команды:**
```powershell
# Создание backup ветки
git checkout -b backup/pre-delete-agents
git add .
git commit -m "Backup перед удалением агентов"

# Возврат на рабочую ветку
git checkout remove-agents

# Удаление файлов
Remove-Item src/thermo_agents/sql_generation_agent.py
Remove-Item src/thermo_agents/database_agent.py
Remove-Item src/thermo_agents/results_filtering_agent.py -ErrorAction SilentlyContinue
Remove-Item src/thermo_agents/individual_search_agent.py -ErrorAction SilentlyContinue

# Commit изменений
git add .
git commit -m "Удалены упразднённые агенты (Этап 8)"
```

**Задачи:**
- [ ] Создать backup ветку
- [ ] Проверить отсутствие импортов упразднённых агентов
- [ ] Удалить файлы `sql_generation_agent.py`, `database_agent.py`
- [ ] Удалить файлы `results_filtering_agent.py`, `individual_search_agent.py` (если есть)
- [ ] Прогнать все тесты после удаления

---

### 2. Обновить `main.py`

**Файл:** `main.py`

**Старая версия (до рефакторинга):**
```python
from src.thermo_agents.sql_generation_agent import SQLGenerationAgent
from src.thermo_agents.database_agent import DatabaseAgent
# ...

# Инициализация агентов
sql_agent = SQLGenerationAgent(...)
db_agent = DatabaseAgent(...)
# ...
```

**Новая версия (после рефакторинга):**
```python
"""Главный файл запуска термодинамической системы."""
import asyncio
from pathlib import Path

from src.thermo_agents.thermodynamic_agent import ThermodynamicAgent
from src.thermo_agents.search.sql_builder import SQLBuilder
from src.thermo_agents.search.database_connector import DatabaseConnector
from src.thermo_agents.search.compound_searcher import CompoundSearcher
from src.thermo_agents.filtering.filter_pipeline import FilterPipeline
from src.thermo_agents.filtering.filter_stages import (
    TemperatureFilterStage,
    PhaseSelectionStage,
    ReliabilityPriorityStage
)
from src.thermo_agents.filtering.phase_resolver import PhaseResolver
from src.thermo_agents.aggregation.reaction_aggregator import ReactionAggregator
from src.thermo_agents.aggregation.table_formatter import TableFormatter
from src.thermo_agents.aggregation.statistics_formatter import StatisticsFormatter
from src.thermo_agents.orchestrator import ThermoOrchestrator


def create_orchestrator(db_path: str = "data/thermo.db") -> ThermoOrchestrator:
    """
    Создание и настройка оркестратора термодинамической системы.
    
    Args:
        db_path: Путь к файлу базы данных
        
    Returns:
        Настроенный ThermoOrchestrator
    """
    # LLM для извлечения параметров
    # TODO: Инициализация LLM-клиента (например, OpenAI, Anthropic)
    llm_client = None  # Заменить на реальный клиент
    thermodynamic_agent = ThermodynamicAgent(llm_client)
    
    # Поиск в БД
    sql_builder = SQLBuilder()
    db_connector = DatabaseConnector(db_path)
    compound_searcher = CompoundSearcher(sql_builder, db_connector)
    
    # Конвейер фильтрации
    filter_pipeline = FilterPipeline()
    filter_pipeline.add_stage(TemperatureFilterStage())
    filter_pipeline.add_stage(PhaseSelectionStage(PhaseResolver()))
    filter_pipeline.add_stage(ReliabilityPriorityStage(max_records=1))
    
    # Агрегация и форматирование
    reaction_aggregator = ReactionAggregator(max_compounds=10)
    table_formatter = TableFormatter()
    statistics_formatter = StatisticsFormatter()
    
    # Оркестратор
    orchestrator = ThermoOrchestrator(
        thermodynamic_agent=thermodynamic_agent,
        compound_searcher=compound_searcher,
        filter_pipeline=filter_pipeline,
        reaction_aggregator=reaction_aggregator,
        table_formatter=table_formatter,
        statistics_formatter=statistics_formatter
    )
    
    return orchestrator


async def main():
    """Главная функция для демонстрации работы системы."""
    # Инициализация
    db_path = Path(__file__).parent / "data" / "thermo.db"
    orchestrator = create_orchestrator(str(db_path))
    
    # Пример запроса
    query = "Хлорирование оксида титана при 600-900K"
    print(f"Запрос: {query}\n")
    
    # Обработка
    response = await orchestrator.process_query(query)
    print(response)


if __name__ == "__main__":
    asyncio.run(main())
```

**Задачи:**
- [ ] Обновить импорты в `main.py`
- [ ] Реализовать функцию `create_orchestrator()`
- [ ] Обновить функцию `main()` с примером использования
- [ ] Добавить конфигурацию (например, через `.env` файл)
- [ ] Протестировать запуск

---

### 3. Добавить `tabulate` в `pyproject.toml`

**Файл:** `pyproject.toml`

**Добавление зависимости:**
```toml
[project]
name = "agents_for_david"
version = "2.0.0"
description = "Термодинамическая система с детерминированной фильтрацией"
requires-python = ">=3.10"
dependencies = [
    "pydantic>=2.0",
    "tabulate>=0.9.0",      # Новая зависимость
    # ... остальные зависимости
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "pytest-asyncio>=0.21",
    "pytest-cov>=4.0",
    # ... остальные dev-зависимости
]
```

**Команда установки:**
```powershell
# Переустановка зависимостей
uv pip install -e .

# Проверка установки tabulate
python -c "import tabulate; print(tabulate.__version__)"
```

**Задачи:**
- [ ] Добавить `tabulate>=0.9.0` в зависимости
- [ ] Обновить версию проекта до `2.0.0`
- [ ] Переустановить зависимости через `uv`
- [ ] Проверить работу `tabulate`

---

### 4. Финальное тестирование

**Процедура:**

#### Шаг 1: Unit-тесты
```powershell
# Запуск всех unit-тестов
pytest tests/ -v --cov=src/thermo_agents --cov-report=html

# Проверка покрытия
# Цель: >80% для новых модулей
```

#### Шаг 2: Интеграционные тесты
```powershell
# Запуск интеграционных тестов
pytest tests/integration/ -v

# Проверка всех сценариев:
# - Простые реакции (2 вещества)
# - Средние реакции (4 вещества)
# - Сложные реакции (10 веществ)
# - Граничные случаи
```

#### Шаг 3: End-to-end тест
```powershell
# Запуск главного файла
python main.py

# Ожидаемый результат:
# - Корректный вывод с таблицей (tabulate)
# - Детальная статистика фильтрации
# - Нет ошибок
```

#### Шаг 4: Проверка производительности
```python
import time
import asyncio
from main import create_orchestrator

async def benchmark():
    orchestrator = create_orchestrator()
    
    # Тест: реакция с 10 веществами
    query = "Сложная реакция с 10 веществами при 500-800K"
    
    start = time.time()
    response = await orchestrator.process_query(query)
    elapsed = time.time() - start
    
    print(f"Время обработки: {elapsed:.2f} сек")
    assert elapsed < 5.0, "Превышен лимит времени (5 сек)"

asyncio.run(benchmark())
```

**Цель производительности:** <5 секунд для 10 веществ

**Задачи:**
- [ ] Прогнать все unit-тесты (>80% покрытие)
- [ ] Прогнать все интеграционные тесты
- [ ] Выполнить end-to-end тест
- [ ] Проверить производительность (<5 сек для 10 веществ)
- [ ] Исправить все обнаруженные ошибки

---

### 5. Миграционные проверки

**Контрольный список:**

- [ ] **Функциональность сохранена**
  - Все запросы из старой версии работают в новой
  - Качество результатов не хуже (сравнить вручную на 10 примерах)

- [ ] **Новые возможности работают**
  - Детальная статистика выводится корректно
  - Таблицы форматируются через `tabulate`
  - Поддержка до 10 веществ

- [ ] **Код чистый**
  - Нет импортов упразднённых агентов
  - Нет неиспользуемого кода
  - Все docstrings актуальны

- [ ] **Документация актуальна**
  - `README.md` соответствует новой архитектуре
  - `CLAUDE.md` описывает новый поток
  - Примеры работают

- [ ] **Тесты проходят**
  - Unit-тесты: 100% новых модулей
  - Интеграционные тесты: все сценарии
  - Покрытие: >80%

---

## Артефакты этапа

### Файлы для удаления:
1. `src/thermo_agents/sql_generation_agent.py`
2. `src/thermo_agents/database_agent.py`
3. `src/thermo_agents/results_filtering_agent.py`
4. `src/thermo_agents/individual_search_agent.py`

### Файлы для обновления:
1. `main.py` — полная переработка
2. `pyproject.toml` — добавление `tabulate`, версия 2.0.0

### Файлы для создания:
1. `docs/MIGRATION.md` — инструкция миграции (опционально)

---

## Критерии завершения этапа

✅ **Обязательные:**
1. Все упразднённые файлы удалены
2. `main.py` обновлён и работает
3. `tabulate` добавлен в зависимости
4. Все тесты проходят (unit + integration)
5. Производительность <5 сек для 10 веществ
6. Функциональность сохранена (проверено вручную)

📋 **Дополнительные:**
- Создан `docs/MIGRATION.md`
- Проведено A/B тестирование старой vs новой версии
- Оптимизация производительности (<3 сек)

---

## Риски

| Риск                       | Вероятность | Влияние     | Митигация                                 |
| -------------------------- | ----------- | ----------- | ----------------------------------------- |
| Регрессия функциональности | Средняя     | Критическое | Ручная проверка на примерах, backup ветка |
| Проблемы с зависимостями   | Низкая      | Среднее     | Использование `uv`, фиксированные версии  |
| Плохая производительность  | Низкая      | Среднее     | Benchmark тесты, оптимизация SQL          |

---

## Следующие шаги после завершения

➡️ **Production deployment:**
- Развертывание на production сервере
- Мониторинг производительности
- Сбор обратной связи пользователей

➡️ **Будущие улучшения:**
- Расчёты термодинамических функций (ΔG, ΔH, ΔS)
- Визуализация данных (графики зависимостей от T)
- API для внешних систем
- Кеширование результатов поиска

---

## Чек-лист завершения рефакторинга

**Этап 0:** ✅ Исследование БД завершено  
**Этап 1:** ✅ SQL-генератор реализован  
**Этап 2:** ✅ Модуль поиска работает  
**Этап 3:** ✅ Фильтрация модульная  
**Этап 4:** ✅ Агрегация и форматирование  
**Этап 5:** ✅ Thermodynamic Agent обновлён  
**Этап 6:** ✅ Orchestrator упрощён  
**Этап 7:** ✅ Тестирование и документация  
**Этап 8:** ✅ Миграция и очистка завершены  

🎉 **Рефакторинг завершён успешно!**
