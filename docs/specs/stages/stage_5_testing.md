# Этап 5: Тестирование и документация

**Статус:** Не начат  
**Приоритет:** Высокий  
**Зависимости:** Этап 4

---

## Цель

Обеспечить полное покрытие тестами, провести тестирование производительности и обновить документацию проекта.

---

## Основные задачи

### 5.1. Завершение модульных тестов

**Задачи:**
- Покрытие тестами > 80% для новых модулей
- Тесты граничных случаев для всех компонентов
- Параметризованные тесты для различных сценариев
- Проверка обработки ошибок

**Файлы:**
- Все тесты из этапов 1-4
- Дополнительные edge case тесты

### 5.2. Интеграционное тестирование

**Файл:** `tests/integration/test_full_pipeline.py` (новый)

**Тесты:**
- Полный цикл: запрос пользователя → результат
- Тестирование примеров из спецификации:
  - "Дай таблицу для H2O при 300-600K"
  - "2 W + 4 Cl2 + O2 → 2 WOCl4 при 600-900K"
  - Различные шаги температуры (25K, 50K, 100K, 150K)
- Проверка форматирования вывода

### 5.3. Performance-тесты

**Файл:** `tests/performance/test_calculation_speed.py` (новый)

**Метрики:**
- Время генерации таблицы для одного вещества (< 100ms для 100 точек)
- Время расчёта реакции (< 200ms для 10 температур)
- Время полного цикла обработки запроса (< 2s)
- Нагрузочное тестирование (100 последовательных запросов)

### 5.4. Обновление документации

**Файлы для обновления:**
- `README.md` — описание новых возможностей
- `docs/ARCHITECTURE_V2.md` — обновление архитектуры
- `docs/specs/output_spec.md` — финализация спецификации
- Docstrings во всех новых модулях

**Новая документация:**
- `docs/user_guide.md` — руководство пользователя с примерами
- `docs/api_reference.md` — API reference для новых классов

### 5.5. Проверка качества кода

**Инструменты:**
- `ruff` — линтинг и форматирование
- `mypy` — проверка типов
- `pytest-cov` — покрытие тестами

**Команды:**
```bash
ruff check src tests
mypy src tests
pytest --cov=src --cov-report=html
```

### 5.6. Создание примеров использования

**Файлы:**
- `examples/README.md` — обзор примеров
- `examples/compound_data_demo.py` — демонстрация запросов compound_data
- `examples/reaction_calculation_demo.py` — демонстрация расчёта реакций
- `examples/custom_temperature_step.py` — использование кастомного шага

---

## Критерии приёмки

- ✅ Покрытие тестами > 80% для всех новых модулей
- ✅ Все интеграционные тесты проходят
- ✅ Performance-тесты показывают приемлемую производительность
- ✅ Документация обновлена и полна
- ✅ Код проходит проверку линтером и type checker
- ✅ Примеры использования работают и документированы
- ✅ CI/CD pipeline настроен (если применимо)

---

## Дополнительно

### Регрессионное тестирование
- Убедиться, что старые функции работают без изменений
- Проверить совместимость с существующими примерами

### Документация миграции
- Создать гайд по миграции для пользователей старой версии (если применимо)

---

## Детальные подзадачи

### 5.1.1. Проверка покрытия тестами

**Команда:**
```bash
pytest --cov=src --cov-report=html --cov-report=term
```

**Целевые метрики:**
- `src/thermo_agents/calculations/` > 85%
- `src/thermo_agents/formatting/` > 80%
- `src/thermo_agents/models/extraction.py` > 90%

**Отчёт:**
```bash
# Генерация HTML отчёта
pytest --cov=src --cov-report=html
# Открыть htmlcov/index.html
```

### 5.1.2. Параметризованные тесты

**Пример для temperature_step_k:**
```python
@pytest.mark.parametrize("step_k,expected_count", [
    (25, 13),   # (600-300)/25 + 1 = 13 точек
    (50, 7),    # (600-300)/50 + 1 = 7 точек
    (100, 4),   # (600-300)/100 + 1 = 4 точки
])
def test_table_generation_with_different_steps(calculator, step_k, expected_count):
    """Проверка генерации таблиц с разными шагами"""
    record = get_h2o_record()
    table = calculator.generate_table(record, 300, 600, step_k)
    
    assert len(table.properties) == expected_count
```

### 5.2.1. E2E тесты из спецификации

**Файл:** `tests/integration/test_full_pipeline.py`

**Тесты:**
```python
class TestFullPipeline:
    
    @pytest.mark.asyncio
    async def test_spec_example_1_h2o(self, orchestrator):
        """Пример 1 из спецификации: H2O при 300-600K"""
        query = "Дай таблицу для H2O при 300-600K"
        result = await orchestrator.process_query(query)
        
        # Проверка структуры вывода
        assert "📊 Термодинамические данные: H2O" in result
        assert "Базовые свойства:" in result
        assert "Формула: H2O" in result
        assert "Фаза: g (gas)" in result
        assert "H298:" in result
        assert "S298:" in result
        
        # Проверка таблицы
        assert "T(K)" in result
        assert "Cp" in result
        assert "300" in result
        assert "600" in result
    
    @pytest.mark.asyncio
    async def test_spec_example_2_w_chlorination(self, orchestrator):
        """Пример 2: Реакция хлорирования вольфрама"""
        query = "2 W + 4 Cl2 + O2 → 2 WOCl4 при 600-900K"
        result = await orchestrator.process_query(query)
        
        # Проверка структуры
        assert "⚗️ Термодинамический расчёт реакции" in result
        assert "Уравнение:" in result
        assert "Метод расчёта:" in result
        assert "Термодинамические данные веществ:" in result
        assert "Результаты расчёта" in result
        
        # Проверка наличия всех веществ
        assert "W" in result
        assert "Cl₂" in result or "Cl2" in result
        assert "O₂" in result or "O2" in result
        assert "WOCl₄" in result or "WOCl4" in result
        
        # Проверка результатов
        assert "ΔH°" in result
        assert "ΔS°" in result
        assert "ΔG°" in result
    
    @pytest.mark.asyncio
    @pytest.mark.parametrize("step_k", [25, 50, 100, 150])
    async def test_custom_temperature_steps(self, orchestrator, step_k):
        """Тест различных шагов температуры"""
        query = f"Свойства CO2 при 400-600K с шагом {step_k} градусов"
        result = await orchestrator.process_query(query)
        
        assert f"Шаг по температуре: {step_k} K" in result
```

### 5.3.1. Benchmark тесты

**Файл:** `tests/performance/test_calculation_speed.py`

**Тесты:**
```python
import pytest
import time

class TestPerformance:
    
    def test_single_table_generation_speed(self, calculator, benchmark):
        """Benchmark генерации таблицы для одного вещества"""
        record = get_h2o_record()
        
        def generate():
            return calculator.generate_table(record, 300, 600, 100)
        
        result = benchmark(generate)
        
        # Проверка времени < 100ms
        assert benchmark.stats['mean'] < 0.1  # секунды
    
    def test_reaction_calculation_speed(self, calculator, benchmark):
        """Benchmark расчёта реакции"""
        W = get_w_record()
        Cl2 = get_cl2_record()
        O2 = get_o2_record()
        WOCl4 = get_wocl4_record()
        
        def calculate():
            return calculator.calculate_reaction_properties(
                reactants=[(W, 2), (Cl2, 4), (O2, 1)],
                products=[(WOCl4, 2)],
                T=600.0
            )
        
        result = benchmark(calculate)
        assert benchmark.stats['mean'] < 0.01  # < 10ms
    
    @pytest.mark.slow
    def test_load_100_queries(self, orchestrator):
        """Нагрузочный тест: 100 последовательных запросов"""
        queries = [
            "Дай таблицу для H2O при 300-600K",
            "CO2 + H2 → CO + H2O при 500-800K",
            "Свойства NH3 при 400-700K"
        ] * 34  # 102 запроса
        
        start = time.time()
        
        for query in queries[:100]:
            result = asyncio.run(orchestrator.process_query(query))
            assert result  # Не пустой результат
        
        duration = time.time() - start
        
        # Среднее время < 2s на запрос
        avg_time = duration / 100
        assert avg_time < 2.0
```

### 5.4.1. Обновление README.md

**Добавить раздел:**
```markdown
## Новые возможности (v2.1)

### Два типа запросов

#### 1. Табличные данные веществ
Получение термодинамических свойств отдельного вещества:
```python
query = "Дай таблицу для H2O при 300-600K с шагом 50 градусов"
result = await orchestrator.process_query(query)
```

#### 2. Расчёт реакций
Термодинамический анализ химических реакций:
```python
query = "2 W + 4 Cl2 + O2 → 2 WOCl4 при 600-900K"
result = await orchestrator.process_query(query)
```

### Параметры

- **Температурный диапазон:** 25-6000K (зависит от данных в базе)
- **Шаг температуры:** 25-250K (по умолчанию 100K)
- **Формат вывода:** Структурированный текст с таблицами и Unicode-символами
```

### 5.4.2. Обновление ARCHITECTURE_V2.md

**Добавить секцию:**
```markdown
## Новые компоненты (v2.1)

### ThermodynamicCalculator
- **Назначение:** Детерминированный расчёт H(T), S(T), G(T)
- **Метод:** Численное интегрирование по формулам Шомейта
- **Производительность:** < 100ms для 100 температурных точек

### Форматтеры
- **CompoundDataFormatter:** Вывод данных по веществам
- **ReactionCalculationFormatter:** Вывод расчётов реакций
- **Unicode-поддержка:** Химические формулы с подстрочными индексами

### Маршрутизация в Orchestrator
- Классификация запросов: `compound_data` vs `reaction_calculation`
- Автоматический выбор форматтера
```

### 5.4.3. Создание user_guide.md

**Файл:** `docs/user_guide.md`

**Содержание:**
```markdown
# Руководство пользователя ThermoSystemV2

## Быстрый старт

### Установка
```bash
pip install -e .
```

### Основные запросы

#### Получение данных по веществу
```python
# Базовый запрос
"Дай таблицу для H2O при 300-600K"

# С кастомным шагом
"Свойства WCl6 с шагом 50 градусов при 400-1000K"
```

#### Расчёт реакции
```python
# Явное уравнение реакции
"2 W + 4 Cl2 + O2 → 2 WOCl4 при 600-900K"

# Описание процесса
"Рассчитай термодинамику хлорирования вольфрама"
```

## Примеры вывода
[Копировать примеры из output_spec.md]
```

### 5.5.1. Проверка линтером

**Команда:**
```bash
ruff check src tests --fix
```

**Конфигурация (.ruff.toml):**
```toml
line-length = 100
target-version = "py310"

[lint]
select = [
    "E",   # pycodestyle errors
    "W",   # pycodestyle warnings
    "F",   # pyflakes
    "I",   # isort
    "B",   # flake8-bugbear
    "C4",  # flake8-comprehensions
]
ignore = ["E501"]  # line too long (handled by formatter)
```

### 5.5.2. Проверка типов mypy

**Команда:**
```bash
mypy src tests --strict
```

**Конфигурация (pyproject.toml):**
```toml
[tool.mypy]
python_version = "3.10"
strict = true
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true
```

### 5.6.1. Создание examples/README.md

**Файл:** `examples/README.md`

**Содержание:**
```markdown
# Примеры использования

## Структура

- `compound_data_demo.py` — Запросы данных по веществам
- `reaction_calculation_demo.py` — Расчёт термодинамики реакций
- `custom_temperature_step.py` — Кастомный шаг температуры
- `advanced_filtering.py` — Фильтрация по фазе и температуре

## Запуск примеров

```bash
# Активировать виртуальное окружение
uv venv
source .venv/bin/activate  # Linux/Mac
# или
.venv\Scripts\Activate.ps1  # Windows

# Запустить пример
python examples/compound_data_demo.py
```
```

### 5.6.2. Создание примера с кастомным шагом

**Файл:** `examples/custom_temperature_step.py`

**Код:**
```python
"""
Демонстрация использования кастомного шага температуры.
"""
import asyncio
from src.thermo_agents.orchestrator import Orchestrator

async def main():
    orchestrator = Orchestrator(...)
    
    # Шаг 25K для детальной таблицы
    query1 = "H2O при 300-400K каждые 25 кельвинов"
    print("=" * 60)
    print("Детальная таблица (шаг 25K):")
    print("=" * 60)
    result1 = await orchestrator.process_query(query1)
    print(result1)
    
    # Шаг 150K для обзорной таблицы
    query2 = "CO2 при 400-1000K с шагом 150 градусов"
    print("\n" + "=" * 60)
    print("Обзорная таблица (шаг 150K):")
    print("=" * 60)
    result2 = await orchestrator.process_query(query2)
    print(result2)

if __name__ == "__main__":
    asyncio.run(main())
```

---

## Финальный чеклист

### Перед выпуском

- [ ] Все тесты проходят (`pytest`)
- [ ] Покрытие > 80% (`pytest --cov`)
- [ ] Линтер без ошибок (`ruff check`)
- [ ] Type checker без ошибок (`mypy`)
- [ ] Документация актуальна
- [ ] Примеры работают
- [ ] Performance тесты в пределах нормы
- [ ] CHANGELOG.md обновлён
- [ ] Создан git tag для релиза

### После выпуска

- [ ] Мониторинг ошибок в production
- [ ] Сбор обратной связи от пользователей
- [ ] Планирование следующих фич

---

