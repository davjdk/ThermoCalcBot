# Спецификация рефакторинга: Термодинамические AI-агенты v2.0

**Статус**: Рабочий MVP → Фундаментальная база  
**Цель**: Повышение единообразия, модульности, лаконичности и элегантности кода  
**Приоритеты**: Поддерживаемость > Производительность > Расширяемость

---

## Общие принципы рефакторинга

### Что сохраняем
✅ Гибридная архитектура (LLM + детерминированная логика)  
✅ Функциональность всех компонентов  
✅ API оркестратора (`ThermoOrchestrator.process_request`)  
✅ Pydantic модели данных  
✅ Структура тестов  

### Что улучшаем
🔧 Единообразие стиля кода и именования  
🔧 Модульность и разделение ответственности  
🔧 Лаконичность и читаемость  
🔧 Документация и типизация  
🔧 Обработка ошибок  

---

## 1. УДАЛЕНИЕ ИЗБЫТОЧНОГО КОДА

### 1.1. Неиспользуемые модели в ThermodynamicAgent

**Проблема**: В `thermodynamic_agent.py` определены модели, которые не используются в текущей архитектуре v2.0:
- `ExtractedParameters` (устаревшая, заменена на `ExtractedReactionParameters`)
- `IndividualSearchRequest` (не используется, поиск идёт через CompoundSearcher)
- `IndividualCompoundResult` (не используется, результаты через `CompoundSearchResult`)
- `AggregatedResults` (не используется, результаты через `AggregatedReactionData`)

**Решение**: ❌ УДАЛИТЬ все неиспользуемые модели из `thermodynamic_agent.py`

**Файл**: `src/thermo_agents/thermodynamic_agent.py` (строки 26-78)

**Риски**: Нет, модели не используются в коде

**План миграции**:
1. Проверить отсутствие импортов этих моделей
2. Удалить определения классов
3. Запустить все тесты для подтверждения

---

### 1.2. Устаревший SQL_GENERATION_PROMPT

**Проблема**: В `prompts.py` содержится детальный промпт `SQL_GENERATION_PROMPT` (841 строка), который больше не используется, так как SQL генерируется детерминированно через `SQLBuilder`.

**Решение**: ❌ УДАЛИТЬ `SQL_GENERATION_PROMPT` или пометить как @deprecated

**Файл**: `src/thermo_agents/prompts.py` (строки 19-71)

**Риски**: Низкие, промпт не используется в текущей архитектуре

**Альтернатива**: Перенести в `docs/archive/` для исторической справки

---

### 1.3. Дублирование логики в Operations

**Проблема**: `operations.py` содержит `Operation` и `OperationLogger`, но частично дублирует функциональность `SessionLogger`.

**Решение**: 🔧 ОБЪЕДИНИТЬ `OperationLogger` и `SessionLogger` в единый компонент логирования

**Файл**: 
- `src/thermo_agents/operations.py`
- `src/thermo_agents/thermo_agents_logger.py`

**Риски**: Средние, требуется рефакторинг всех точек использования

**План миграции**:
1. Проанализировать использование `OperationLogger` (сейчас не используется активно)
2. Перенести функциональность в `SessionLogger`
3. Удалить `OperationLogger` или сделать thin wrapper над `SessionLogger`
4. Обновить `SessionLogger` для поддержки структурированных операций

---

## 2. УЛУЧШЕНИЕ АРХИТЕКТУРЫ

### 2.1. Упрощение AgentStorage

**Проблема**: `AgentStorage` реализует сложную систему сообщений (Message Queue), но в текущей архитектуре используется минимально. Основное взаимодействие происходит напрямую через методы.

**Решение**: 🔧 УПРОСТИТЬ `AgentStorage` до простого Key-Value хранилища с поддержкой TTL

**Мотивация**: 
- Текущая реализация избыточна для MVP
- Message Queue можно добавить позже при необходимости
- Упрощение уменьшит complexity

**Файл**: `src/thermo_agents/agent_storage.py`

**Риски**: Средние, требуется рефакторинг ThermodynamicAgent

**План рефакторинга**:
1. Сохранить методы `set()`, `get()`, `delete()`, `exists()`, `clear()`
2. Удалить или упростить `send_message()`, `receive_messages()`, `_message_queue`
3. Оставить только простой словарь для session state
4. Обновить `ThermodynamicAgent` для работы без Message Queue
5. Добавить комментарий о возможном расширении до полноценной MQ в будущем

---

### 2.2. Единообразие именования фаз

**Проблема**: Несогласованность в именовании полей фазовых переходов:
- В `DatabaseRecord`: `tmelt`, `tboil` (алиасы `MeltingPoint`, `BoilingPoint`)
- В реальной БД: `MeltingPoint`, `BoilingPoint`
- В коде фильтрации: смесь обоих подходов

**Решение**: 🔧 СТАНДАРТИЗИРОВАТЬ на snake_case везде в Python коде

**Мотивация**: PEP 8, единообразие кодовой базы

**Файлы**:
- `src/thermo_agents/models/search.py` (DatabaseRecord)
- `src/thermo_agents/filtering/phase_resolver.py`
- `src/thermo_agents/filtering/temperature_resolver.py`

**План миграции**:
1. Использовать `tmelt`, `tboil` как primary, `MeltingPoint`, `BoilingPoint` как алиасы
2. Обновить все использования в коде фильтрации
3. Обновить тесты

---

### 2.3. Консолидация импортов

**Проблема**: Множественные относительные импорты и дублирование:
```python
from src.thermo_agents.models.search import CompoundSearchResult  # Absolute
from ..models.search import DatabaseRecord  # Relative
```

**Решение**: 🔧 СТАНДАРТИЗИРОВАТЬ на абсолютные импорты

**Мотивация**: Согласно инструкциям в `.github/copilot-instructions.md`

**Файлы**: Все `*.py` в `src/thermo_agents/`

**План миграции**:
1. Заменить все `from ..` на `from src.thermo_agents.`
2. Заменить все `from .` на `from src.thermo_agents.<module>.`
3. Настроить linter для проверки (ruff/flake8)

---

### 2.4. Улучшение обработки ошибок в FilterPipeline

**Проблема**: `FilterPipeline` имеет сложную логику fallback с множественными проверками и условиями (200+ строк в `_apply_fallback` и related methods).

**Решение**: 🔧 РЕФАКТОРИТЬ fallback-логику в отдельные стратегии

**Мотивация**: Single Responsibility Principle, улучшение тестируемости

**Файл**: `src/thermo_agents/filtering/filter_pipeline.py`

**План рефакторинга**:
1. Создать `FallbackStrategy` protocol/abstract class
2. Реализовать конкретные стратегии:
   - `IonicRecordsFallback`
   - `CompositeFormulaFallback`
   - `TopRecordsFallback`
3. Внедрить стратегии в `FilterPipeline` через композицию
4. Упростить метод `_apply_fallback()` до вызова стратегий

**Новая структура**:
```python
# src/thermo_agents/filtering/fallback_strategies.py
class FallbackStrategy(Protocol):
    def apply(self, context: FilterContext, records: List[DatabaseRecord]) -> List[DatabaseRecord]: ...

class IonicRecordsFallback(FallbackStrategy): ...
class CompositeFormulaFallback(FallbackStrategy): ...
class TopRecordsFallback(FallbackStrategy): ...
```

---

### 2.5. Упрощение PhaseBasedTemperatureStage

**Проблема**: `PhaseBasedTemperatureStage` объединяет слишком много логики (температура + фаза + scoring + выбор), что затрудняет понимание и тестирование.

**Решение**: 🔧 РАЗДЕЛИТЬ на два отдельных компонента:
- `TemperatureFilterStage` - фильтрация по температуре
- `PhaseSelectionStage` - умный выбор фазы с учётом скоринга

**Мотивация**: Single Responsibility, улучшение читаемости

**Файл**: `src/thermo_agents/filtering/phase_based_temperature_stage.py`

**План рефакторинга**:
1. Извлечь температурную логику в `TemperatureFilterStage`
2. Извлечь логику выбора фазы в `PhaseSelectionStage`
3. Настроить конвейер для использования обеих стадий последовательно
4. Обновить тесты для обеих стадий отдельно

---

## 3. ПОВЫШЕНИЕ ЧИТАЕМОСТИ

### 3.1. Документация модулей

**Проблема**: Неполная документация в некоторых модулях, отсутствие примеров использования.

**Решение**: ✏️ ДОБАВИТЬ docstrings в формате Google Style ко всем публичным классам и методам

**Файлы**: Все модули в `src/thermo_agents/`

**Стандарт**:
```python
def method(self, param: str) -> Result:
    """Краткое описание метода.
    
    Подробное описание функциональности метода,
    включая edge cases и особенности реализации.
    
    Args:
        param: Описание параметра
        
    Returns:
        Описание возвращаемого значения
        
    Raises:
        ValueError: Когда и почему
        
    Example:
        >>> result = method("test")
        >>> result.status
        'success'
    """
```

---

### 3.2. Упрощение сложных условий

**Проблема**: Множественные вложенные условия в фильтрации затрудняют понимание логики.

**Примеры**:
- `filter_pipeline.py`: `_prefilter_exclude_ions()` с вложенными if
- `phase_resolver.py`: `_determine_phase()` с сложной логикой
- `reaction_validator.py`: Nested conditions в `validate_reaction()`

**Решение**: 🔧 УПРОСТИТЬ через early returns и извлечение условий в методы

**До**:
```python
if condition1:
    if condition2:
        if condition3:
            return result
        else:
            return alternative
    else:
        return other
else:
    return default
```

**После**:
```python
if not condition1:
    return default
if not condition2:
    return other
if not condition3:
    return alternative
return result
```

---

### 3.3. Константы вместо магических чисел

**Проблема**: Использование магических чисел в коде фильтрации и scoring.

**Примеры**:
- `0.6`, `0.4` в `PhaseBasedTemperatureStage` (веса)
- `100`, `200`, `1500` в `PhaseResolver` (температурные пороги)
- `3` в `fallback` (top-N записей)

**Решение**: 🔧 ИЗВЛЕЧЬ в именованные константы в начале модулей

**После**:
```python
# Temperature thresholds for phase estimation (Kelvin)
SOLID_PHASE_MAX_TEMP = 200
LIQUID_PHASE_TYPICAL_MIN = 273
LIQUID_PHASE_TYPICAL_MAX = 373
GAS_PHASE_MIN_TEMP = 1500

# Scoring weights
DEFAULT_RELIABILITY_WEIGHT = 0.6
DEFAULT_COVERAGE_WEIGHT = 0.4

# Fallback configuration
FALLBACK_TOP_RECORDS_COUNT = 3
```

---

## 4. УЛУЧШЕНИЕ ТИПИЗАЦИИ

### 4.1. Добавление Protocol для абстракций

**Проблема**: Некоторые компоненты не имеют явных интерфейсов, что затрудняет mock testing и расширение.

**Решение**: 🔧 ДОБАВИТЬ Protocol definitions для ключевых абстракций

**Новые файлы**: `src/thermo_agents/protocols.py`

```python
from typing import Protocol, List, Tuple, Optional
from .models.search import CompoundSearchResult, DatabaseRecord

class CompoundSearcherProtocol(Protocol):
    """Протокол для поиска соединений в базе данных."""
    
    def search_compound(
        self,
        formula: str,
        temperature_range: Optional[Tuple[float, float]] = None,
        phase: Optional[str] = None,
        limit: int = 100
    ) -> CompoundSearchResult: ...

class FilterStageProtocol(Protocol):
    """Протокол для стадии фильтрации."""
    
    def filter(
        self,
        records: List[DatabaseRecord],
        context: FilterContext
    ) -> List[DatabaseRecord]: ...
    
    def get_stage_name(self) -> str: ...
```

**Мотивация**: Явные контракты, упрощение тестирования, документация интерфейсов

---

### 4.2. Строгая типизация в AgentStorage

**Проблема**: `AgentStorage` использует `Any` для значений, что снижает типобезопасность.

**Решение**: 🔧 ДОБАВИТЬ generics для типизированного хранилища

**До**:
```python
def get(self, key: str, default: Any = None) -> Any:
```

**После**:
```python
from typing import TypeVar, Generic, Optional

T = TypeVar('T')

def get(self, key: str, default: Optional[T] = None) -> Optional[T]:
```

---

## 5. ОПТИМИЗАЦИЯ ПРОИЗВОДИТЕЛЬНОСТИ

### 5.1. Кэширование в CommonCompoundResolver

**Проблема**: Каждый запрос создаёт словари и проверяет паттерны заново.

**Решение**: 🔧 ДОБАВИТЬ @lru_cache для методов проверки

**Файл**: `src/thermo_agents/search/common_compounds.py`

```python
from functools import lru_cache

@lru_cache(maxsize=256)
def is_common_compound(self, formula: str) -> bool:
    """Cached check for common compounds."""
    return formula.upper() in self._exact_matches
```

**Ожидаемый эффект**: Ускорение на 10-20% для повторяющихся запросов

---

### 5.2. Ленивая инициализация в DatabaseConnector

**Проблема**: Соединение с БД открывается при создании объекта, даже если не используется.

**Решение**: 🔧 РЕАЛИЗОВАТЬ lazy connection через property

**Файл**: `src/thermo_agents/search/database_connector.py`

**До**:
```python
def __init__(self, db_path: str):
    self.connection = sqlite3.connect(db_path)
```

**После**:
```python
def __init__(self, db_path: str):
    self.db_path = db_path
    self._connection: Optional[sqlite3.Connection] = None

@property
def connection(self) -> sqlite3.Connection:
    if self._connection is None:
        self._connection = sqlite3.connect(self.db_path)
    return self._connection
```

---

## 6. СТРУКТУРНЫЕ ИЗМЕНЕНИЯ

### 6.1. Реорганизация prompts.py

**Проблема**: 841 строка промптов в одном файле, сложно навигировать.

**Решение**: 🔧 РАЗДЕЛИТЬ на несколько файлов по назначению

**Новая структура**:
```
src/thermo_agents/prompts/
├── __init__.py
├── extraction.py       # THERMODYNAMIC_EXTRACTION_PROMPT
├── legacy.py           # Устаревшие промпты (SQL_GENERATION_PROMPT)
└── manager.py          # PromptManager
```

---

### 6.2. Создание config модуля

**Проблема**: Конфигурация разбросана по разным классам (ThermoAgentConfig, OrchestratorConfig, FilterPriorities).

**Решение**: 🔧 СОЗДАТЬ централизованный config модуль

**Новый файл**: `src/thermo_agents/config.py`

```python
from dataclasses import dataclass
from typing import Optional

@dataclass
class DatabaseConfig:
    """Конфигурация подключения к БД."""
    db_path: str = "data/thermo_data.db"
    connection_timeout: int = 30

@dataclass
class LLMConfig:
    """Конфигурация LLM."""
    api_key: str
    base_url: str = "https://openrouter.ai/api/v1"
    model: str = "openai/gpt-4o"
    max_retries: int = 4
    timeout: int = 60

@dataclass
class SystemConfig:
    """Главная конфигурация системы."""
    llm: LLMConfig
    database: DatabaseConfig
    log_level: str = "INFO"
    logs_dir: str = "logs/sessions"
    
    @classmethod
    def from_env(cls) -> 'SystemConfig':
        """Загрузка из переменных окружения."""
        ...
```

---

## 7. ПЛАН МИГРАЦИИ

### Фаза 1: Безопасное удаление (1-2 дня)
- [ ] 1.1. Удалить неиспользуемые модели из thermodynamic_agent.py
- [ ] 1.2. Удалить/архивировать SQL_GENERATION_PROMPT
- [ ] Запустить все тесты, убедиться в работоспособности

### Фаза 2: Рефакторинг логирования (2-3 дня)
- [ ] 1.3. Объединить OperationLogger и SessionLogger
- [ ] Обновить все точки использования
- [ ] Написать/обновить тесты для нового логгера

### Фаза 3: Упрощение AgentStorage (2-3 дня)
- [ ] 2.1. Упростить AgentStorage до Key-Value
- [ ] Обновить ThermodynamicAgent
- [ ] Обновить тесты

### Фаза 4: Стандартизация (3-4 дня)
- [ ] 2.2. Унифицировать именование (tmelt/tboil)
- [ ] 2.3. Стандартизировать импорты (абсолютные)
- [ ] 3.3. Извлечь магические числа в константы
- [ ] 3.1. Добавить docstrings

### Фаза 5: Архитектурные улучшения (5-7 дней)
- [ ] 2.4. Рефакторинг fallback в стратегии
- [ ] 2.5. Разделить PhaseBasedTemperatureStage
- [ ] 3.2. Упростить сложные условия
- [ ] 4.1. Добавить Protocol definitions

### Фаза 6: Оптимизация (2-3 дня)
- [ ] 4.2. Улучшить типизацию AgentStorage
- [ ] 5.1. Добавить кэширование
- [ ] 5.2. Ленивая инициализация DatabaseConnector

### Фаза 7: Структурные изменения (3-4 дня)
- [ ] 6.1. Реорганизация prompts
- [ ] 6.2. Создание config модуля
- [ ] Финальное тестирование и документация

**Общая оценка**: 18-26 дней (3-4 недели)

---

## 8. КРИТЕРИИ УСПЕХА

### Метрики качества кода
- [ ] Снижение cyclomatic complexity на 20%
- [ ] Увеличение test coverage до 85%+
- [ ] 0 ошибок линтера (ruff/flake8)
- [ ] Все публичные API документированы

### Метрики производительности
- [ ] Время обработки не увеличилось (<=5 сек для 10 веществ)
- [ ] Использование памяти не увеличилось
- [ ] Все существующие тесты проходят

### Метрики архитектуры
- [ ] Количество строк кода сократилось на 10-15%
- [ ] Количество файлов с >500 строками: 0
- [ ] Coupling между модулями снижен (измерить через radon)

---

## 9. РИСКИ И МИТИГАЦИИ

| Риск                                        | Вероятность | Влияние | Митигация                                   |
| ------------------------------------------- | ----------- | ------- | ------------------------------------------- |
| Поломка существующей функциональности       | Средняя     | Высокое | Полное покрытие тестами перед рефакторингом |
| Увеличение времени обработки                | Низкая      | Среднее | Бенчмарки до и после каждой фазы            |
| Несовместимость с внешними интеграциями     | Низкая      | Среднее | Сохранение публичных API неизменными        |
| Увеличение сложности из-за over-engineering | Средняя     | Среднее | Code review каждого PR, следование YAGNI    |

---

## 10. ПРИМЕРЫ "ДО И ПОСЛЕ"

### Пример 1: AgentStorage упрощение

**До** (сложная Message Queue):
```python
storage.send_message(
    source_agent="thermo",
    target_agent="searcher",
    message_type="request",
    payload={"formula": "H2O"},
    correlation_id=corr_id
)
messages = storage.receive_messages(target_agent="searcher")
```

**После** (простой Key-Value):
```python
storage.set(f"search_request_{corr_id}", {"formula": "H2O"})
request = storage.get(f"search_request_{corr_id}")
```

---

### Пример 2: Fallback стратегии

**До** (200+ строк в одном методе):
```python
def _apply_fallback(self, initial_records, ionic_records, prefilter_applied, context):
    # 50 строк проверки ионных записей
    # 50 строк проверки составных формул
    # 50 строк fallback на top-N
    # 50 строк логирования
    ...
```

**После** (чистая композиция):
```python
def _apply_fallback(self, context: FilterContext, records: List[DatabaseRecord]) -> List[DatabaseRecord]:
    for strategy in self.fallback_strategies:
        result = strategy.apply(context, records)
        if result:
            return result
    return []
```

---

## ЗАКЛЮЧЕНИЕ

Данная спецификация описывает путь от **рабочего MVP** к **фундаментальной базе** проекта. Рефакторинг направлен на:

1. **Удаление технического долга** (неиспользуемый код, дублирование)
2. **Повышение единообразия** (импорты, именование, стиль)
3. **Улучшение модульности** (разделение ответственности, протоколы)
4. **Повышение читаемости** (документация, упрощение условий)

Результат: **Чистая, поддерживаемая, элегантная кодовая база**, готовая к дальнейшему развитию термодинамических расчётов и масштабированию.

**Следующий шаг**: Создание ветки `refactor/architecture-improvements` и начало реализации по фазам.