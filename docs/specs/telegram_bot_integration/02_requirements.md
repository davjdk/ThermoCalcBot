# Функциональные и технические требования

**Проект:** ThermoSystem Telegram Bot Integration
**Версия документа:** 1.1
**Дата:** 9 ноября 2025

---

## 🎯 1. Функциональные требования

### 1.1. Основные команды бота

| Команда | Описание | Обязательность | Пример использования |
|---------|----------|----------------|---------------------|
| `/start` | Приветствие и краткая справка | Обязательно | `/start` |
| `/help` | Подробная справка по использованию | Обязательно | `/help` |
| `/calculate <query>` | Выполнить термодинамический расчёт | Обязательно | `/calculate 2 H2 + O2 → 2 H2O` |
| `/status` | Статус бота и текущая нагрузка | Обязательно | `/status` |
| `/examples` | Примеры запросов | Обязательно | `/examples` |
| `/about` | Информация о системе | Обязательно | `/about` |

### 1.2. Прямые текстовые запросы

Бот должен обрабатывать естественные языковые запросы без префикса `/calculate`:

**Поддерживаемые типы запросов:**

1. **Таблицы свойств веществ:**
   - `"Дай таблицу для H2O при 300-600K с шагом 50 градусов"`
   - `"Свойства CO2 от 298 до 1000K"`
   - `"Термодинамические данные для CH4 при 400-800K"`

2. **Расчёты реакций:**
   - `"2 H2 + O2 → 2 H2O при 298-1000K"`
   - `"Реакция горения метана при 500K"`
   - `"Термодинамика реакции NH3 синтеза"`

3. **Аналитические запросы:**
   - `"Реагирует ли сероводород с оксидом железа(II) при 500-700°C?"`
   - `"Условия для реакции CO + H2O → CO2 + H2"`
   - `"Равновесие реакции при различных температурах"`

### 1.3. Форматирование ответов

#### 1.3.1. Основные требования к форматированию

**Ограничения Telegram API:**
- Максимальный размер сообщения: 4096 символов
- Поддерживаемые форматы: Markdown, HTML
- Поддержка Unicode символов (H₂O, CO₂, →, ΔH)

**Адаптация вывода:**
1. **Разделение длинных сообщений** (>4096 символов)
2. **Markdown форматирование** для таблиц и формул
3. **Unicode символы** для химических формул
4. **Эмодзи** для визуальной структуры 🔥⚗️📊
5. **Прогресс индикаторы** для долгих расчётов (>5 секунд)

#### 1.3.2. Пример формата ответа

```markdown
🔥 *Термодинамический расчёт реакции*

**Уравнение:** 2 H₂ + O₂ → 2 H₂O
**Температурный диапазон:** 298K - 1000K
**Фаза продуктов:** Жидкая при T < 373K, Газовая при T > 373K

📊 *Результаты расчёта:*

| T (K) | ΔH (kJ/mol) | ΔS (J/mol·K) | ΔG (kJ/mol) | ln(K)  | K        |
| ----- | ----------- | ------------ | ----------- | ------ | -------- |
| 298   | -571.66     | -326.67      | -474.36     | 191.42 | 2.1×10⁸³ |
| 400   | -577.23     | -318.12      | -450.18     | 135.34 | 3.8×10⁵⁸ |

📊 *Интерпретация:*
✓ Реакция экзотермическая (ΔH < 0)
✓ Реакция самопроизвольная (ΔG < 0)
✓ Константа равновесия K >> 1
```

#### 1.3.3. Эмодзи и визуальная структура

| Тип контента | Эмодзи | Применение |
|--------------|--------|------------|
| Реакции | 🔥 | Заголовки расчётов реакций |
| Таблицы | 📊 | Заголовки таблиц с данными |
| Информация | ℹ️ | Справочная информация |
| Предупреждения | ⚠️ | Важные замечания |
| Ошибки | ❌ | Сообщения об ошибках |
| Успех | ✅ | Успешное завершение |
| Прогресс | ⏳ | Индикаторы обработки |

### 1.4. Поддержка TXT файлов

#### 1.4.1. Функциональные требования к файлам

**Технические возможности:**
- ✅ **Поддержка TXT файлов** до 20MB (лимит Telegram Bot API)
- ✅ **UTF-8 кодировка** - сохранение Unicode символов
- ✅ **Метод `sendDocument()`** с `InputFile` классом
- ✅ **Профессиональные отчёты** для исследований

**Автоматическая логика отправки:**
- `< 3000 символов` → отправка как Telegram сообщение
- `≥ 3000 символов` или `большие таблицы` → отправка как TXT файл
- `> 20MB` → предупреждение и предложение уменьшить диапазон

#### 1.4.2. Структура TXT файлов

```text
================================================================================
                      ТЕРМОДИНАМИЧЕСКИЙ РАСЧЁТ РЕАКЦИИ
================================================================================

Уравнение реакции: 2 H₂ + O₂ → 2 H₂O
Температурный диапазон: 298.15K - 1000.00K
Шаг по температуре: 50.00K
Количество точек: 15

================================================================================
                              ИСХОДНЫЕ ДАННЫЕ
================================================================================

Реагенты:
1. H₂ (Водород)
   - Фаза: Газовая (g)
   - Tₘᵢₙ-Tₘₐₓ: 298.15K - 1000.00K
   - Записей: 3 (Reliability Class: 1)

2. O₂ (Кислород)
   - Фаза: Газовая (g)
   - Tₘᵢₙ-Tₘₐₓ: 298.15K - 1000.00K
   - Записей: 3 (Reliability Class: 1)

Продукты:
1. H₂O (Вода)
   - Фаза: Жидкая (l) при T < 373.15K
   - Фаза: Газовая (g) при T ≥ 373.15K
   - Tₘᵢₙ-Tₘₐₓ: 273.15K - 1000.00K
   - Записей: 4 (Reliability Class: 1)

================================================================================
                            РЕЗУЛЬТАТЫ РАСЧЁТА
================================================================================

      T (K)     ΔH (kJ/mol)    ΔS (J/mol·K)    ΔG (kJ/mol)      ln(K)           K
    --------  --------------  --------------  --------------  -----------  -------------
     298.15        -571.66         -326.67         -474.36        191.42      2.13e+83
    ─────────────────────────────────────────────────────────────────────────────────────
    ⚡ ФАЗОВЫЙ ПЕРЕХОД: H₂O(l) → H₂O(g) при T = 373.15K
    ─────────────────────────────────────────────────────────────────────────────────────
     398.15        -576.78         -319.12         -449.68        135.76      1.01e+59

================================================================================
                             ИНТЕРПРЕТАЦИЯ
================================================================================

Термодинамический анализ реакции:
✓ Реакция экзотермическая (ΔH < 0) во всём диапазоне температур
✓ Реакция самопроизвольная (ΔG < 0) при всех температурах
✓ Константа равновесия K >> 1, реакция протекает практически полностью
✓ Увеличение температуры смещает равновесие в сторону реагентов

Фазовые переходы:
- При T = 373.15K: H₂O(l) → H₂O(g) (фазовый переход учитывается в расчётах)

================================================================================
                          МЕТОДИКА РАСЧЁТА
================================================================================

1. Источник данных: База термодинамических данных (316,434 записей)
2. Метод расчёта: Формулы Шомейта с численным интегрированием
3. Программное обеспечение: ThermoSystem v2.2
4. Дата расчёта: 2025-11-09 10:30:22
5. Погрешность: ±5% (зависит от надёжности исходных данных)

Сгенерировано ThermoSystem Telegram Bot @ThermoCalcBot
================================================================================
```

### 1.5. Обработка ошибок

| Тип ошибки | Обработка | Сообщение пользователю |
|------------|-----------|------------------------|
| Неверные формулы | Предложения исправлений | `❌ *Неверная формула*<br>Проверьте химическую формулу: H2O вместо H20` |
| Отсутствие данных | Альтернативные вещества | `⚠️ *Данные не найдены*<br>Попробуйте: CO2 вместо CO₂ или укажите фазу` |
| Timeout запросов | Повторные попытки (max 3) | `⏳ *Обработка...*<br>Запрос сложный, повторная попытка` |
| Системные ошибки | Логирование и уведомление админов | `😔 *Системная ошибка*<br>Попробуйте позже или /help` |
| Превышение лимитов | Объяснение ограничений | `⚠️ *Слишком большой запрос*<br>Уменьшите температурный диапазон` |

---

## 🔧 2. Технические требования

### 2.1. Требования к окружению

#### 2.1.1. Базовые требования

- **Python:** 3.12+
- **Память:** 1GB+ (для работы с базой данных)
- **Сеть:** Доступ к OpenRouter API и Telegram Bot API
- **База данных:** SQLite файл `data/thermo_data.db`
- **Файловая система:** Временная директория для TXT файлов

#### 2.1.2. Зависимости и библиотеки

**Основные зависимости:**
```toml
[project.optional-dependencies]
telegram = [
    "python-telegram-bot>=20.7",
]
```

**Существующие зависимости ThermoSystem:**
- `pydantic` - Валидация данных и модели
- `asyncio` - Асинхронная обработка
- `sqlite3` - Работа с базой данных
- `openai` - LLM API (через OpenRouter)

**Примечание:** Rate limiting реализуется с помощью встроенных `asyncio.Semaphore` и механизмов `python-telegram-bot`, дополнительные библиотеки не требуются.

#### 2.1.3. Установка зависимостей

```bash
# Установка зависимостей для Telegram бота
uv sync --group telegram

# Проверка установки
uv run python -c "import telegram; print('✅ python-telegram-bot installed')"
```

### 2.2. Конфигурационные требования

#### 2.2.1. Обязательные переменные окружения (.env)

```bash
# Telegram Bot Configuration
TELEGRAM_BOT_TOKEN=[ТОКЕН_БОТА]  # Обязательно
TELEGRAM_BOT_USERNAME=ThermoCalcBot
TELEGRAM_WEBHOOK_URL=https://your-domain.com/webhook/telegram  # Для production
TELEGRAM_MODE=polling  # polling или webhook

# Bot Configuration
MAX_CONCURRENT_USERS=20  # Консервативное значение для начального запуска
REQUEST_TIMEOUT_SECONDS=60
MESSAGE_MAX_LENGTH=4000
RATE_LIMIT_REQUESTS_PER_MINUTE=30
```

#### 2.2.2. Дополнительные переменные окружения

```bash
# File Configuration
ENABLE_FILE_DOWNLOADS=true
AUTO_FILE_THRESHOLD=3000
FILE_CLEANUP_HOURS=24
MAX_FILE_SIZE_MB=20  # Лимит Telegram Bot API
TEMP_FILE_DIR=temp/telegram_files

# Admin Configuration
TELEGRAM_ADMIN_USER_ID=123456789  # Optional
LOG_BOT_ERRORS=true

# ThermoSystem Configuration (существующие)
OPENROUTER_API_KEY=[API_КЛЮЧ]
LLM_BASE_URL=https://openrouter.ai/api/v1
LLM_DEFAULT_MODEL=openai/gpt-4o
DB_PATH=data/thermo_data.db
LOG_LEVEL=INFO
```

### 2.3. Требования к API интеграции

#### 2.3.1. Telegram Bot API интеграция

**Режимы работы:**

**Development - Polling режим:**
```python
# Для разработки и тестирования
application.run_polling()
```

**Production - Webhook режим:**
```python
# Для продакшена
application.run_webhook(
    listen="0.0.0.0",
    port=8443,
    url_path="telegram",
    webhook_url="https://your-domain.com/webhook/telegram"
)
```

**Требования к Webhook:**
- SSL сертификат (Let's Encrypt для production)
- Nginx reverse proxy
- Ответ в течение 30 секунд
- Проверка подписи запросов (security)

#### 2.3.2. ThermoSystem API интеграция

**Основной интерфейс:**
```python
async def process_query(self, user_query: str) -> str
```

**Требования к интеграции:**
- Полная совместимость с `ThermoOrchestrator v2.2`
- Адаптация вывода под Telegram ограничения
- Обработка Unicode символов химических формул
- Асинхронная обработка с прогресс индикаторами

### 2.4. Требования к производительности

#### 2.4.1. Временные характеристики

| Операция | Максимальное время | Целевое время |
|----------|-------------------|---------------|
| Простой расчёт (1-2 вещества) | 30 секунд | < 5 секунд |
| Сложный расчёт (5+ веществ) | 60 секунд | < 10 секунд |
| Форматирование ответа | 5 секунд | < 2 секунды |
| Отправка TXT файла | 15 секунд | < 5 секунд |

#### 2.4.2. Требования к масштабируемости

**Начальные требования:**
- **Пользователи:** 20 одновременных пользователей
- **Запросы:** 30 запросов в минуту (лимит Telegram API)
- **Память:** < 500MB для одного экземпляра бота
- **CPU:** < 50% при максимальной нагрузке

**Потенциальное масштабирование:**
- **Пользователи:** до 100 одновременных
- **Запросы:** до 100 в минуту
- **Горизонтальное масштабирование:** поддержка нескольких экземпляров

#### 2.4.3. Требования к надёжности

**Доступность:**
- **Uptime:** 99.9%
- **Graceful degradation:** при высокой нагрузке
- **Автовосстановление:** после сбоев
- **Резервное копирование:** базы данных и логов

**Обработка ошибок:**
- **Timeout:** автоматические повторные попытки
- **Fallback ответы:** при недоступности LLM
- **Rate limiting:** защита от DDoS
- **Circuit breaker:** при критических ошибках

### 2.5. Требования к безопасности

#### 2.5.1. Защита токена и данных

**Обязательные меры:**
- **Токен бота:** только в переменных окружения
- **Отсутствие токена:** в коде, git, логах
- **.env файл:** не должен коммититься
- **Доступ к переменным:** ограниченный

**Защита данных пользователей:**
- **Логирование:** только ID пользователей (не имён)
- **Сессии:** хранение в зашифрованном виде
- **Удаление:** старых логов регулярно
- **GDPR compliance:** при работе с EU пользователями

#### 2.5.2. Валидация и санитизация

**Входные данные:**
- **Длина запроса:** максимум 1000 символов
- **Запрещенные паттерны:** HTML/JS код, SQL injection
- **Химические формулы:** валидация формата
- **Специальные символы:** эскейпинг для Markdown

**Выходные данные:**
- **Разделение сообщений:** при >4096 символов
- **Markdown валидация:** перед отправкой
- **Unicode обработка:** для совместимости
- **Файлы:** проверка размера и типа

---

## 📋 3. Нефункциональные требования

### 3.1. Требования к usability

- **Интуитивный интерфейс:** понятные команды и сообщения
- **Быстрые ответы:** прогресс индикаторы для долгих операций
- **Качественное форматирование:** таблицы, формулы, Unicode
- **Подробная помощь:** `/help` с примерами использования
- **Обработка ошибок:** понятные сообщения с предложениями

### 3.2. Требования к поддерживаемости

- **Модульная архитектура:** разделение компонентов
- **Детальное логирование:** всех операций и ошибок
- **Конфигурация:** через переменные окружения
- **Документация:** полная и актуальная
- **Тесты:** unit, integration, E2E тесты

### 3.3. Требования к мониторингу

- **Health checks:** проверка БД и LLM API
- **Метрики:** количество запросов, время ответа, ошибки
- **Алерты:** критические ситуации
- **Логи:** структурированные и поисковые
- **Dashboard:** визуализация метрик

---

## 📊 4. Критерии приёмки (Acceptance Criteria)

### 4.1. Функциональные критерии

- [ ] **Все команды бота работают корректно:** `/start`, `/help`, `/calculate`, `/status`, `/examples`, `/about`
- [ ] **Текстовые запросы обрабатываются:** без префикса `/calculate`
- [ ] **Результаты адаптируются:** под 4096 символов limitation
- [ ] **TXT файлы генерируются:** для отчётов >3000 символов
- [ ] **Unicode символы сохраняются:** H₂O, CO₂, →, ΔH
- [ ] **Ошибки обрабатываются:** с полезными сообщениями

### 4.2. Технические критерии

- [ ] **Время ответа <10 секунд** для сложных расчётов
- [ ] **Поддержка 20+ одновременных пользователей**
- [ ] **99.9% uptime** без критических сбоев
- [ ] **Безопасное хранение** токена в .env
- [ ] **Детальное логирование** всех операций
- [ ] **Health checks** для БД и LLM API

### 4.3. Интеграционные критерии

- [ ] **Полная совместимость** с ThermoOrchestrator v2.2
- [ ] **Асинхронная обработка** всех запросов
- [ ] **SessionLogger интеграция** для отладки
- [ ] **Обработка фазовых переходов** в расчётах
- [ ] **Professionally formatted TXT** отчёты

### 4.4. Критерии качества

- [ ] **Unit тесты** для всех основных компонентов
- [ ] **Integration тесты** для API интеграции
- [ ] **E2E тесты** для пользовательских сценариев
- [ ] **Performance тесты** для нагрузки
- [ ] **Security тесты** для уязвимостей

---

## 📈 5. Следующие шаги

После ознакомления с требованиями перейдите к документу **[03_architecture_design.md](./03_architecture_design.md)** для изучения системной архитектуры и дизайна компонентов.

---

**Документ подготовлен для:** Команды разработки ThermoSystem
**Целевая аудитория:** Python разработчики, System Architects
**Сложность реализации:** Средняя (требует знаний async, Telegram API, security)