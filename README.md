# AI Agents Project v2.1

Проект термодинамических AI-агентов с гибридной архитектурой (LLM + детерминированная логика) для анализа химических соединений и реакций.

## 🎯 Обзор проекта

Система использует многоагентную архитектуру v2.1 для обработки термодинамических запросов:

- **Thermodynamic Agent** (LLM) - Извлечение параметров из запросов пользователей
- **Детерминированные модули** - Поиск, фильтрация и агрегация данных без LLM
- **Thermo Orchestrator v2.1** - Интеллектуальная маршрутизация запросов
- **Новые форматтеры** - Профессиональный вывод с поддержкой Unicode

## 🆕 Новые возможности (v2.1)

### Два типа запросов

#### 1. Табличные данные веществ
Получение термодинамических свойств отдельного вещества:
```python
query = "Дай таблицу для H2O при 300-600K с шагом 50 градусов"
result = await orchestrator.process_query(query)
```

#### 2. Расчёт реакций
Термодинамический анализ химических реакций:
```python
query = "2 W + 4 Cl2 + O2 → 2 WOCl4 при 600-900K"
result = await orchestrator.process_query(query)
```

### Ключевые улучшения
- **Intelligent Routing**: Автоматическая классификация запросов
- **ThermodynamicCalculator**: Детерминированные расчёты по формулам Шомейта
- **Unicode Support**: Химические формулы с подстрочными индексами (H₂O)
- **Professional Tables**: Форматирование через tabulate
- **Performance**: < 100ms для генерации таблиц, < 2s для полного цикла

### Параметры
- **Температурный диапазон:** 25-6000K (зависит от данных в базе)
- **Шаг температуры:** 25-250K (по умолчанию 100K)
- **Формат вывода:** Структурированный текст с таблицами и Unicode-символами

## 🏗️ Архитектура v2.0

### Поток обработки
```
User Query → Thermodynamic Agent (LLM) → CompoundSearcher → FilterPipeline → ReactionAggregator → TableFormatter → Response
```

### Замененные агенты
| Оригинальный агент | Новая реализация | Преимущества |
|------------------|----------------|-------------|
| SQL Generation Agent | SQLBuilder | Детерминированная логика, многоуровневый поиск |
| Database Agent | DatabaseConnector | Надёжное соединение, статистика |
| Results Filtering Agent | FilterPipeline (6 стадий) | Прозрачная логика, конвейерная обработка |
| Response Formatter | TableFormatter + StatisticsFormatter | Форматирование через tabulate |

## 🚀 Требования

- Python 3.12+
- uv для управления зависимостями
- OpenRouter API ключ (для LLM)

## 📦 Установка

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd agents-for-david
```

2. Установите зависимости с помощью uv:
```bash
uv sync
```

3. Активируйте виртуальное окружение:
```bash
uv shell
```

4. Настройте переменные окружения в `.env`:
```env
OPENROUTER_API_KEY=your_api_key_here
LLM_BASE_URL=https://openrouter.ai/api/v1
LLM_DEFAULT_MODEL=openai/gpt-4o
DB_PATH=data/thermo_data.db
LOG_LEVEL=INFO
```

## 🏃 Запуск

```bash
uv run python main.py
```

## 💻 Интерактивный режим

После запуска вы попадете в интерактивный режим:
```
THERMO AGENTS v2.0 - Interactive Mode
Using hybrid architecture: LLM + deterministic modules

Commands:
  • Type your thermodynamic query
  • 'status' - Show system status
  • 'clear' - Clear message history
  • 'exit' - Exit the system
```

### Примеры запросов
- `H2O + O2 -> H2O2 при 298K`
- `Термодинамические данные для Fe, TiO2, Al2O3`
- `Реакция горения метана при температуре 500-1000K`

## 📊 База данных

Система использует SQLite базу с **316,434 записями** и **32,790 уникальных химических формул**:
- 100% покрытие температурных диапазонов (Tmin/Tmax)
- 74.66% данных высшего качества (ReliabilityClass = 1)
- Поддержка фазовых переходов (Tmelt/Tboil)
- Многоуровневый поиск для сложных соединений

## 🧪 Работа с ноутбуками

- Зависимости для ноутбуков уже добавлены (`ipykernel`, `pandas`).
- Для работы в виртуальном окружении активируйте оболочку или используйте `uv run`.

Открыть и выполнить ноутбук `db_work.ipynb`:

```bash
# Активировать окружение (опционально)
uv shell
# Запустить Jupyter (если нужно)
uv run python -m ipykernel
```

В VS Code выберите ядро `.venv (Python 3.12)` для ноутбука. Затем выполните все ячейки.

## 📚 Примеры использования

### Базовое использование
```bash
# Запуск базовой демонстрации
uv run python examples/basic_usage.py
```

### Расширенная фильтрация
```bash
# Демонстрация различных стратегий фильтрации
uv run python examples/advanced_filtering.py
```

### Кастомные форматтеры
```bash
# Экспорт данных в различные форматы
uv run python examples/custom_formatters.py
```

### Интеграционные тесты
```bash
# Запуск сквозных тестов
uv run pytest tests/integration/test_end_to_end.py -v

# Запуск тестов граничных случаев
uv run pytest tests/integration/test_edge_cases.py -v
```

## 📁 Структура проекта

```
src/thermo_agents/
├── agent_storage.py              # Хранилище A2A коммуникации
├── thermodynamic_agent.py        # LLM агент извлечения параметров
├── orchestrator.py               # Оркестратор v2.0
├── prompts.py                    # Системные промпты
├── thermo_agents_logger.py       # Система логирования
├── search/                       # Детерминированный поиск
│   ├── sql_builder.py           # Генерация SQL запросов
│   ├── database_connector.py    # Соединение с БД
│   └── compound_searcher.py     # Поиск соединений
├── filtering/                    # Конвейерная фильтрация
│   ├── filter_pipeline.py       # Конвейер из 6 стадий
│   ├── filter_stages.py         # Реализация стадий
│   ├── temperature_resolver.py  # Разрешение температур
│   ├── phase_resolver.py        # Определение фаз
│   └── complex_search_stage.py  # Комплексный поиск
├── aggregation/                  # Агрегация данных
│   ├── reaction_aggregator.py   # Агрегатор реакций
│   ├── table_formatter.py       # Форматирование таблиц
│   └── statistics_formatter.py  # Форматирование статистики
└── models/                       # Pydantic модели
    ├── search.py                # Модели поиска
    ├── aggregation.py           # Модели агрегации
    └── extraction.py            # Модели извлечения

data/
└── thermo_data.db                # База термодинамических данных

tests/
├── integration/                  # Интеграционные тесты
│   ├── test_end_to_end.py      # Сквозные тесты (20+ сценариев)
│   └── test_edge_cases.py      # Граничные случаи (15+ тестов)
└── unit/                        # Unit тесты для модулей

examples/                         # Примеры использования
├── basic_usage.py               # Базовое использование системы
├── advanced_filtering.py        # Расширенные стратегии фильтрации
└── custom_formatters.py         # Кастомные форматтеры (CSV, JSON, HTML)

docs/
├── specs/stages/                # Спецификации этапов рефакторинга
├── database_analysis_report.md  # Детальный анализ БД (316K записей)
└── database_analysis_results.json # Результаты анализа

logs/sessions/                    # Логи сессий с детальной трассировкой
```

## 🔄 Этапы рефакторинга

### ✅ Завершенные этапы (6/8)

| Этап | Статус | Длительность | Ключевые достижения |
|------|--------|--------------|---------------------|
| **Этап 0** | ✅ Завершён | 1 день | Комплексный анализ 316K записей БД |
| **Этап 1** | ✅ Завершён | 1 день | Детерминированный SQL Builder |
| **Этап 2** | ✅ Завершён | 1 день | Search Module с DatabaseConnector |
| **Этап 3** | ✅ Завершён | 1 день | FilterPipeline с 6 стадиями фильтрации |
| **Этап 4** | ✅ Завершён | 1 день | ReactionAggregator, TableFormatter |
| **Этап 5** | ✅ Завершён | 1 день | Обновлённый ThermodynamicAgent |
| **Этап 6** | ✅ Завершён | 1 день | **РЕФАКТОРИНГ ORCHESTRATOR** |
| **Этап 7** | ✅ Завершён | 1 день | **ТЕСТИРОВАНИЕ И ДОКУМЕНТАЦИЯ** |
| **Этап 8** | 🔴 Не начат | 1-2 дня | Миграция и очистка |

### 🎯 Этап 6: Рефакторинг Orchestrator (ЗАВЕРШЁН)

**Реализованные изменения:**
- ✅ Полная замена message-passing архитектуры на прямые вызовы
- ✅ Интеграция детерминированных модулей (SQLBuilder, FilterPipeline, ReactionAggregator)
- ✅ Удаление зависимостей от упразднённых агентов
- ✅ Форматирование ответов через `tabulate`
- ✅ Вывод детальной статистики фильтрации
- ✅ Поддержка до 10 веществ в реакции

**Новый поток обработки:**
```python
user_query → thermodynamic_agent.extract_parameters()
    → for compound in compounds:
        search_result = compound_searcher.search_compound()
        filter_result = filter_pipeline.execute()
    → aggregated_data = reaction_aggregator.aggregate_reaction_data()
    → formatted_response = table_formatter.format_summary_table()
```

### 🎯 Этап 7: Тестирование и документация (ЗАВЕРШЁН)

**Реализованные изменения:**
- ✅ **20+ интеграционных тестов** для реальных химических запросов
- ✅ **15+ тестов граничных случаев** для обработки ошибок
- ✅ **Обновлен CLAUDE.md** с новой архитектурой v2.0
- ✅ **Созданы примеры использования** в папке `examples/`
- ✅ **Подтвержден анализ БД** из Этапа 0

**Тестовое покрытие:**
- Сквозные тесты: Простые, средние, сложные реакции (2-10 веществ)
- Граничные случаи: Экстремальные температуры, отсутствующие вещества, ошибки
- Форматирование: CSV, JSON, HTML, Markdown экспорт
- Производительность: <5 секунд для 10 соединений

**Созданные артефакты:**
- `tests/integration/test_end_to_end.py` - 20+ сквозных тестов
- `tests/integration/test_edge_cases.py` - 15+ тестов граничных случаев
- `examples/basic_usage.py` - Базовое использование системы
- `examples/advanced_filtering.py` - Расширенные стратегии фильтрации
- `examples/custom_formatters.py` - Кастомные форматтеры данных

## 🛠️ Разработка

Проект использует uv для управления зависимостями и виртуальным окружением.
Для добавления новых зависимостей используйте:

```bash
uv add package-name
```

Для разработческих зависимостей:

```bash
uv add --dev package-name
```

## 📈 Метрики проекта

- **Строк кода**: ~8000+ (новая детерминированная логика)
- **Тесты**: 103+ unit-тестов, покрытие >80%
- **Интеграционные тесты**: 35+ тестов (20+ сквозных, 15+ граничных случаев)
- **Производительность**: <5 сек для 10 веществ
- **Надёжность**: 74.66% данных высшего качества

## 🔧 Конфигурация

Основные параметры в `.env`:
- `OPENROUTER_API_KEY` - API ключ для LLM
- `LLM_BASE_URL` - OpenRouter endpoint
- `LLM_DEFAULT_MODEL` - Модель по умолчанию
- `DB_PATH` - Путь к базе данных
- `LOG_LEVEL` - Уровень логирования

## 📄 Лицензия

Проект для обучения и разработки AI агентов.

---

**Версия:** 2.0
**Обновлено:** 15 октября 2025
**Следующий этап:** Миграция и очистка (Этап 8)